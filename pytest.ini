[pytest]
# -rs: every artifact-backed suite under tests/e2e skips itself
# when its PLANNER_* variable names no built path, and the reason is the only
# thing that tells that apart from a run with nothing to say.
addopts = -rs
