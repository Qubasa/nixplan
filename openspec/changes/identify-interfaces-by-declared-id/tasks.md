## 1. Ordering and registration while the change is in flight

- [x] 1.1 Confirm the prerequisite is in: verify `hold-declaration-shape-and-fold-set-reads/tasks.md` has every task checked, that `lib/interface.nix` carries `fold ? null` and `foldOf`, and that `nix eval --json .#planner.failures` is `[]`. Do not start section 2 before this holds - `foldName` and `foldApply` are written against a field that change introduces
- [x] 1.2 `git add openspec/changes/identify-interfaces-by-declared-id/` so the flake can see the change, and add the four new spec paths to `excused` in `tests/unit/coverage.nix` with the precedent reason used for `declare-service-state` ("an unimplemented change: no task of identify-interfaces-by-declared-id has been done, so nothing in this package claims to satisfy it yet"); verify `nix eval --json .#planner.suites.coverage.testEverySpecificationIsClassified.expr` reports empty `unclassified`, `vanished` and `unreadable`

## 2. Identity in the library

- [x] 2.1 Add `id ? null` to the `interface` constructor in `lib/interface.nix:47-54` and return it beside `name`, `exports` and `fold`; verify a throwaway `nix eval --expr` builds an interface with and without an `id` and that `nix eval --json .#planner.failuresBySuite.interfaces` is `[]`
- [x] 2.2 Add a `fold` constructor beside `interface` and `unitExtension`: `fold = name: apply: { inherit name apply; }`, and add `foldName` and `foldApply`, each accepting both a bare function (name `null`, apply the function) and a named fold; export `fold` from `lib/default.nix` in the `korora` attrset and in the library's own attributes (`lib/default.nix:57-70`); verify a throwaway `nix eval --expr` shows `foldApply` returning the same function for both spellings and `foldName` returning `null` for the bare one
- [x] 2.3 Add `identityOf = iface: { id, exports, fold }` to `lib/interface.nix`, where `exports` maps each export name to `{ type = <the korora type's `name`>; secrecy = secrecyOf atom; }` and `fold` is `foldName` of the declared fold; return `null` for an interface whose claim is absent or refused, so a null identity can never compare equal to another; verify a throwaway `nix eval --expr` shows `identityOf` carrying no function at any depth by running it through `builtins.toJSON`
- [x] 2.4 Use the korora type's `name` and not `__name`: verify by a throwaway `nix eval --expr` that `identityOf` distinguishes an export typed `attrsOf string` from one typed `attrsOf int`, which `__name` would call `attrsOf` in both cases
- [x] 2.5 Add `interface-id-malformed` beside the existing atom checks: a declared `id` that is not a non-empty string free of whitespace is an error row, subject `interface.subjectOf reg iface`, message naming the interface and what was written, evidence stating that an identity is claimed with a string two authors can both write, resolution naming the declaring file; the interface then falls back to value identity; verify an `interfaces` suite test asserts the id, subject and severity, and a second asserts that a wire between that interface and its own value still resolves
- [x] 2.6 Add `interface-fold-name-malformed`: a named fold whose `name` is not a non-empty whitespace-free string is an error row against the interface's declaring file, and the fold is not applied - a set-valued read then delivers the provider-keyed set unchanged; verify an `interfaces` suite test asserts the row and a `resolution` suite test asserts the unfolded value reaching the consumer
- [x] 2.7 Add `interface-id-unnamed-fold`: an interface declaring an `id` and a fold carrying no name is an error row against its declaring file, and its claim is disregarded so it falls back to value identity; verify an `interfaces` suite test asserts the row and that `identityOf` of that interface is `null`
- [x] 2.8 Add `interface-id-unnamespaced` as a **warning**: an `id` carrying neither `.` nor `/` names the interface and the `id`, states that the namespace is shared with every author, and resolves to a qualified form; the claim still identifies; verify an `interfaces` suite test asserts severity `warning`, that `applicable` stays `true`, and that a wire matching on that `id` still resolves
- [x] 2.9 Add the registry-level `interface-id-conflict` to `registryRows` (`lib/interface.nix:174-175`): two attributed interfaces claiming one `id` with differing identities produce one error row, subjected to the first of the two declaring files by sort order and naming the second in its evidence, with the evidence stating exactly what differs - an export present on one side only, an export's two type names, an export's two secrecies, or two fold names; verify an `interfaces` suite test asserts the row for each of those four differences and that the subject is the same on two evaluations

## 3. Matching at the wire

- [x] 3.1 Replace `interfaceMatches` (`lib/resolve.nix:916-917`) with: both ends carry a non-null `identityOf` and those identities are equal, or the two values are equal. Value equality stays the fallback and is unchanged when either end claims nothing; verify `nix eval --json .#planner.worked.plan` still equals `fixtures/minimal-typed-edge/plan/backup.json` field for field
- [x] 3.2 Emit `interface-id-conflict` at the wire when both ends claim one `id` and their identities differ, with the consuming entry as subject, and refuse the edge - the slot is undelivered and absent from `results`, the same way a mismatch is today; verify a `resolution` suite test asserts the row, that the slot is absent from the values the consumer receives, and that the plan is still produced for every other entry
- [x] 3.3 Confirm the two rows do not both fire for one conflict: the wire row and the registry row are the same id, subject and message for one pair, so `dedup` keeps one; verify a `resolution` suite test builds a deployment where a conflicting pair is both attributed and wired and asserts exactly one row in the table
- [x] 3.4 Extend the `interface-mismatch` evidence (`lib/resolve.nix:1022-1027`) with the two new branches: both ends claim an `id` and the claims differ, and the two are different values while at most one claims an `id`; keep the existing same-name branch; verify a `resolution` suite test asserts the evidence text of each of the three branches and that the row's id and severity are unchanged
- [x] 3.5 Confirm a matched claim changes nothing downstream: verify a `resolution` suite test in which two interfaces match by claim across two evaluations and asserts that the consumer receives only the exports its slot named, that a generated value's delivery set is the one the declared read produces, and that a provider publishing a value its own type refuses still produces the export type row against the provider

## 4. Attribution

- [x] 4.1 Give `fileOf` (`lib/interface.nix:75-80`) an identity fallback: value equality first, then the first registry entry whose `identityOf` is non-null and equal; leave `label` and `subjectOf` unchanged above it; verify an `interfaces` suite test asserts that a row about an interface built by a second evaluation prints the declaring file of the attributed interface claiming the same identity, and that an unattributed interface still renders the "declaring file not recorded" text

## 5. The plan field

- [x] 5.1 Carry the claimed identity through the resolved capability record (`lib/resolve.nix:367-371`, beside `declaringFile` and `interfaceName`) and write it into the plan at `lib/plan.nix:157-160`, omitting the attribute entirely when no claim was made; verify `nix eval --json .#planner.worked.plan` still equals `fixtures/minimal-typed-edge/plan/backup.json` field for field, because no in-tree interface claims one
- [x] 5.2 Assert the field in the `plan` suite: an entry providing a capability whose interface claims an `id` records it beside the name and the declaring file; an entry whose interface claims none carries no such attribute; adopting a claim leaves every entry key unchanged; verify each of the three fails when the attribute is written unconditionally or folded into the key

## 6. Two evaluations as evidence

- [x] 6.1 Give the `interfaces` suite the library source it needs to build a second evaluation: add `libSource` to its arguments in `tests/default.nix:21` the way the `diagnostics` suite already receives it, and expose a helper in `tests/unit/support.nix` that returns a second, independent `import libSource { inherit korora systems; }`; verify a throwaway `nix eval --expr` shows an interface of one shape built through each evaluation comparing unequal by value
- [x] 6.2 Pin the defect this change exists for: a test asserting that two interfaces of one shape built by two evaluations, over an atom this library owns, are unequal as values and equal as identities, and that a wire between them resolves; verify the test fails when `interfaceMatches` is reverted to value equality alone

## 7. Scenario coverage

- [x] 7.1 Move the four spec paths from `excused` to `accountable` in `tests/unit/coverage.nix`; verify `nix eval --json .#planner.failuresBySuite.coverage` now names the unmapped scenario headings rather than the unclassified files
- [x] 7.2 Map every `planner/interface-identity` scenario to the `interfaces`, `resolution` and `plan` test names from sections 2 to 6, adding any test the mapping reports missing; verify the coverage suite reports nothing outstanding for that spec
- [x] 7.3 Map the `planner/typed-edge` scenarios to the tests from section 3, including the two scenarios the delta rewrote; verify the coverage suite reports nothing outstanding for that spec
- [x] 7.4 Map the `planner/interface-fold` scenarios to the tests from tasks 2.2 and 2.6 and the `planner/plan-artifact` scenarios to task 5.2; verify `nix eval --json .#planner.failuresBySuite.coverage` is `[]`

## 8. Documentation

- [x] 8.1 Document `id` in the interface section of `docs/authoring.md:14-48`: that it is optional, that it is a claim rather than a registration, that both ends must claim for a claim to be used, what an identity is made of, and that an unqualified one is warned about; verify the section agrees with `lib/interface.nix` field for field
- [x] 8.2 Document the named fold beside `fold` in `docs/authoring.md`, including that a claim requires one and an unclaimed interface does not; verify no other part of that file still implies a fold has no name
- [x] 8.3 Add `fold` and `id` to the library export table in `docs/README.md:191-206`, and the capability record's new field to `docs/plan.md:203-204`; verify the plan document's rendered example matches the fixture it quotes
- [x] 8.4 Add the five new rows to `docs/diagnostics.md` in their sections with their message, evidence and resolution shapes, and update the `interface-mismatch` entry with its three evidence branches; verify every id the library can emit appears exactly once in that file
- [ ] 8.5 Record the invariants in `CLAUDE.md` under "Interfaces, composition, reads": an interface is identified by the value an author imported **or** by an identity it claims with an `id`; a claim is the `id`, the export shape and the fold's name, and carries no function so it survives two evaluations of the library; both ends must claim; a refused claim falls back to the value; and `name` is still a label. Verify the file still names every registration point it names today

## 9. Measurement and close-out

- [ ] 9.1 Re-measure the nine budgets with `perf/measure.sh` and re-pin `perf/budgets.json` only if the ratchet fires, recording interpreter version and date with the new figures; verify `nix eval --json .#planner.failuresBySuite.perf` is `[]`
- [ ] 9.2 Confirm the tree is green and the goldens did not move: verify `nix eval --json .#planner.failures` is `[]`, `nix eval --json .#planner.worked.plan` equals `fixtures/minimal-typed-edge/plan/backup.json`, and `nix eval --raw .#planner.rendered` equals `fixtures/minimal-typed-edge/plan/diagnostics.txt`
- [ ] 9.3 Run the formatter once, at the end: `nix fmt`, then verify `nix build .#checks.<system>.treefmt` passes
