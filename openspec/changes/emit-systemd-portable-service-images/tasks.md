## 1. Baseline

- [x] 1.1 Record today's behaviour as a throwaway probe: a scenario module writing `units.web.after`, `units.web.oneShot`, `units.web.user` and `units.db.env.PGDATA` beside a differing `units.web.env.PGDATA`, plus a package interpolated only into a `configData` string; verify the plan records only `command` for both units, that the `env` disagreement is `unit-environment-conflict`, and that the config-only package is absent from `closure` — paste all three observations into the task as the before-state
  - before-state, measured by `nix eval .#lib --apply` over the probe (`/tmp/claude-outputs/probe-1-1.nix`):
    - `unitFieldNames = { db = [ "command" ]; web = [ "command" ]; }` — `after`, `oneShot`, `user` and both `env` sets are dropped from the record entirely
    - `rowIds = [ "unit-environment-conflict" ]` with message ``two units of probe:only@one declare different values for the environment variable `PGDATA` ``, `applicable = false`, and the entry-level `env` is last-write-wins `{ PGDATA = "/var/lib/db"; }`
    - `closure = [ ".../borgbackup-1.4.0" ]` and `configOnlyPackageInClosure = false` — the openssh path interpolated only into `configData` is absent
- [x] 1.2 Capture the current perf measurement with `bash perf/measure.sh` and keep its output; verify `nix build .#checks.x86_64-linux.planner-perf -L` passes against the committed `perf/budgets.json` before anything changes
  - baseline measurement kept at `~/.claude/outputs/planner-perf-baseline/` (built through `.#planner-perf-results`, which runs `measure.sh` in the sandbox); `nix build .#checks.x86_64-linux.planner-perf -L` passed with `0 failures, 0 invalid comparisons` and every gated counter of every fixture at 0.0% headroom against the committed budgets

## 2. Types and the extension constructor

- [x] 2.1 Add the atom types the unit vocabulary needs beside `url` and `secretRef` in `lib/atoms.nix` — a unit reference, a duration, a schedule and a user name; verify a nix-unit case asserts each type accepts a well-formed value and rejects a malformed one through korora's non-raising entry point
  - `unitRef`, `duration`, `schedule` and `userName` in `lib/atoms.nix`; `tests/suites/units.nix::testTheVocabularyAtomTypesVerify` asserts one accepted and one rejected value per type through `type.verify`
- [x] 2.2 Add `unitExtension { backend, name, fields }` to `lib/interface.nix` beside `interface`, sharing `atomRows`' key-and-type checking (`:95-141`), and export it from `lib/default.nix`; verify a case asserts two extensions declared in different files with one `name` are told apart, and that a field declaring a key outside `{ type }` is a row
  - the shared reader is `interface.typedFieldRows`, called by `atomRows` with the export-atom ids and by `readExtension` with the extension ids; `testTwoExtensionsSharingAName` and `testAnExtensionFieldDeclaresAnUnknownKey` cover both halves
  - `interface.registryRows` dispatches the registry by kind, so an extension registered for its declaring-file label is not read as an interface
- [x] 2.3 Add the extension application reader: `extends = [ { extension, values } ]` with `values`' keyset checked as a subset of the extension's `fields`; verify cases assert a partial `values` produces no row, an unknown key produces a row, and a mistyped value produces a row naming the field and the type
  - `testAPartialExtensionApplication`, `testAnUnknownKeyInsideAnExtensionApplication`, `testAMistypedExtensionValue`

## 3. The typed unit vocabulary and per-unit environments

- [x] 3.1 Add the typed unit-record reader beside `readVars` in `lib/module.nix`, reading `command`, `env`, `after`, `requires`, `user`, `oneShot`, `remainAfterExit`, `schedule`, `timeout`, `stopCommand`, `reloadCommand` and `extends`, with a field absent from the record when the unit did not declare it; verify a case asserts a unit declaring three fields records exactly three and that a mistyped `timeout` is a row
  - `module.readUnit`; `testALongRunningUnit`, `testAUnitThatAppliesAndExits`, `testAMistypedFieldValue`
- [x] 3.2 Refuse a unit reference in `after`/`requires` naming a unit the module did not declare, with an error row naming the module and the reference; verify a case asserts the row and that the ordering is absent from the plan
  - `testAUnitReferenceToAUnitTheModuleDoesNotOwn`
- [x] 3.3 Record the vocabulary and the per-backend extension groups in `placedEntry` (`lib/plan.nix:311-377`), replacing the `{ command = unit.command or null; }` projection at `:327`, and add both to `keyInput` (`:334-347`); verify a case asserts changing a unit's `timeout` alone moves that entry's key and no other entry's, and that `requires` and `after` are recorded as distinct fields
  - `tests/suites/units.nix::testAUnitFieldIsInTheEntryKey` and `tests/suites/units.nix::testAnOrderingAndARequirementAreDistinct`
- [x] 3.4 Replace `mergedEnv` (`lib/plan.nix:92-114`) with a per-unit environment on the unit plus an entry-level intersection, delete the `unit-environment-conflict` row and its test, and delete its entry from `docs/diagnostics.md`; verify the probe from 1.1 now records both values with no row, and that a variable both units agree on also appears in the entry's `env`
  - after-state, same probe with the new configuration-file shape and a declared closure (`/tmp/claude-outputs/probe-3-4.nix`): `perUnitPgdata = { db = "/var/lib/db"; web = "/var/lib/web"; }`, `entryEnv = { TZ = "UTC"; }` (the one variable both units agree on), `rowIds = [ ]`, `applicable = true`, `unitFieldNames.web = [ "after" "command" "env" "oneShot" "user" ]`, and `configOnlyPackageInClosure = true`

## 4. Backend targeting

- [x] 4.1 Add `target` to `implArgs` (`lib/resolve.nix:383-389`) carrying the placement machine's platform record and `serviceManager`, and absent for a member no placement selected; verify a case asserts one module placed on a systemd machine and a launchd machine produces two entries whose `extends` differ, and that an unplaced member's implementation sees no `target`
  - `testOneModulePlacedOnTwoServiceManagers`; `testAnUnplacedMemberHasNoTarget` observes the absence the way a module would — the module raises when handed a `target`, so the unplaced member's own rows are `[ "member-not-placed" ]` while the placed one's are `[ "module-raised" ]`
- [x] 4.2 Emit `unit-extension-backend-mismatch` when an extension's `backend` is not the placement target's `serviceManager`, naming the entry, the unit, the extension and both backends; verify a case asserts the row fires when the module forgets the conditional, and that the extension is still recorded under its own backend so a renderer refuses knowingly
  - `testTheSameExtensionOnALaunchdTarget` and its negative `testALaunchdExtensionOnALaunchdTarget`
- [x] 4.3 Update the `impl` argument table in `docs/authoring.md` (`:138-162`) and the exported-symbol table in `docs/README.md` (`:164-175`) for `target` and `unitExtension`; verify the documented argument set matches `implArgs` by reading both
  - `authoring.md:205-225` lists the seven arguments and states `target` is absent rather than null at an unplaced member; `README.md:182` documents `unitExtension` and `:185` the `platform` projection. Both read against `resolve.nix`'s `implArgs`.

## 5. The implementation half is read like the declaration half

- [x] 5.1 Add the implementation-result reader in `lib/resolve.nix`, replacing the unchecked `impl.units or { }` (`:397`) and `impl.configData or { }` (`:421`) with an allow-listed read of the implementation's top level, of each unit, of each extension application and of each configuration file, reusing `module.keyRow`'s excluded-before-unknown dispatch under a distinct `implementation-unknown-key` id; verify a case asserts a raw service-manager stanza written directly on a unit produces the row naming the module, the unit, the key and the vocabulary
  - `testARawServiceManagerStanzaWrittenOnAUnit`, `testAMisspelledVocabularyField`, `testAnUnknownImplementationKey`
  - the reader forces the implementation's shape and its unit set inside a guard, and leaves a configuration file's recipe unforced until its identity is computed, because a recipe rendered over a set with an absent entry has no bytes to force
- [x] 5.2 Add a case asserting reading stays total: one unit with an unrecognised key beside one well-formed unit produces exactly one row and a fully recorded second unit; verify the entry still appears in the plan
  - `testOneMalformedUnitBesideOneWellFormedUnit`
- [x] 5.3 Add a case asserting a key colliding with a name in `lib/excluded.nix` produces the exclusion row with its trigger text rather than the unknown-key row; verify `excluded.constructs` gained no key by re-running `tests/suites/exclusions.nix` unchanged
  - `testAnImplementationKeyNamingAnExcludedConstruct`; `tests/suites/exclusions.nix` is unmodified by this change and green

## 6. Configuration files name their bytes

- [x] 6.1 Rebuild `configDataRecord` (`lib/plan.nix:262-299`) around `mode`, a module-named `reload` replacing the all-units list at `:270`, and exactly one of `source` or `render`; verify a case asserts both dispositions given at once is an error row, and that a file naming no unit records an empty reaction rather than every unit of the entry
  - `testBothDispositionsOnOneFile`, `testNeitherDispositionOnOneFile`, `testAFileReloadingOneOfTwoUnits`
- [x] 6.2 Implement the hashing rule: a `source` file identified by its store path, a `render` list of only `text` items carrying a content hash, and a `render` list carrying any `ref` carrying a hash over fragments and reference paths and no digest over assembled bytes; verify a case asserts a recipe referencing a secret vars path records no content digest, and that grepping the serialised plan for the fixture's secret bytes still finds nothing
  - `testAFileCopiedFromAStorePath`, `testAFileAssembledFromPublicLiterals`, `testARecipeThatReferencesASecretPath` (records `structureHash` and no `contentHash`, and `bytesAnywhereInThePlan = false` over the serialised plan)
- [x] 6.3 Keep the incomplete-set branch intact: a file whose recipe is rendered over a set with an absent entry records neither hash nor recipe and carries the row; verify the existing `set-entry-absent` case still passes against the new record shape
  - `tests/suites/plan.nix::testARenderedFileOverAnIncompleteSet` now expects `{ computed = false; mode = "0600"; reload = [ "borgRepo" ]; row = { id = "set-entry-absent"; … }; }`

## 7. Machines, platforms and the reduced elaboration

- [x] 7.1 Add `system`, `serviceManager` and the optional microarchitecture to `machineRegistryKeys` (`lib/resolve.nix:44-47`) and to `machineRecords` (`:88-91`), with an error row naming the machine and the registry file when a selected machine omits either required key; verify a case asserts the row and that the plan still contains the machine entry and every entry placed on it
  - `testAMachineOmitsItsSystem`, `testAMachineOmitsItsServiceManager`, `testAFullyDeclaredMachine`
- [x] 7.2 Add the reduced platform projection over `lib.systems.elaborate`, allow-listing `system`, `config`, `libc`, `useLLVM`, `linuxArch`, `parsed.cpu`, `parsed.kernel`, `parsed.abi` without its `assertions`, and the codegen group `gcc` (`abi`, `arch`, `cmodel`, `cpu`, `float`, `float-abi`, `fpu`, `long-double-format`, `mode`, `strict-align`, `thumb`, `tune`), memoised per distinct `system` string; verify a case asserts the record is JSON-serialisable and carries no function at any depth, that two machines of one system produce equal records, and that a field outside the allow-list is absent even though the elaboration carries it
  - `lib/platform.nix`, memoised in `resolve.nix` per distinct system and microarchitecture pair; `testAPlatformRecordIsSerialisable`, `testTwoMachinesOfOneSystem`, `testAFieldOutsideTheAllowListIsAbsent`, `testAnAarch64MachinesPlatformRecord`, `testAMachineDeclaresAMicroarchitecture`
  - the `is*` predicates were in this allow-list and are not any more. All 75 are `mapAttrs (n: v: v final.parsed) inspect.predicates` (`nixpkgs:lib/systems/default.nix:442`), so each was a function of `parsed`, which the record already carries: they cost 130 of every entry's 160 target lines, entered every `keyInput`, and told a consumer nothing it could not compute. `testAFieldOutsideTheAllowListIsAbsent` now asserts the record names none of them and derives two from `parsed` instead
  - the same edit widened `gcc` from `arch`/`tune` to the twelve fields cc-wrapper and gcc's own build read, and records the group whenever the elaboration carries any of it rather than only when the machine declared a microarchitecture: `armv7l-linux` records `{ arch = "armv7-a"; fpu = "vfpv3-d16"; }` and `loongarch64-linux` its `cmodel` and `strict-align`, none of which the old two-field list could hold. `testAPlatformCarriesItsOwnCodegenDefaults`
- [x] 7.3 Add the guard against upstream's own filter being reached for instead: a case asserting `(lib.systems.elaborate "aarch64-linux")._withoutFunctions` fails `builtins.toJSON` because two functions survive at `parsed.abi.assertions[*].assertion` (measured against the pinned nixpkgs; `nixpkgs:lib/systems/default.nix:57-65,128` strips top-level names only), plus a case asserting the projection's own field set names nothing in `lib.systems.functionNames`; verify both, and if the first case starts passing, stop and report — upstream fixed it and D7's first measurement needs rewriting rather than the guard deleting
  - `testUpstreamsOwnFilterIsNotAPlanValue` and `testTheProjectionsFieldSetNamesNoFunction`. The surviving functions are the evidence rather than a failed `toJSON`: `builtins.toJSON` of a function is not an error `tryEval` catches, so asking whether it serialises aborts the suite instead of failing a case. The two survivors are still exactly `_withoutFunctions.parsed.abi.assertions[0].assertion` and `[1].assertion`, so upstream has not fixed it and the guard stands.
- [x] 7.4 Cross a module's `platforms` (`lib/module.nix:341`) against each selected machine's `system`, emitting `placement-platform-mismatch` naming instance, member, machine, the machine's system and the module's systems, treating an empty `platforms` as every system; verify a case asserts exactly one row for a tag selecting two machines of which one system is unsupported, and no row for the supported one
  - `testOneTagSelectsTwoSystems`, `testAModuleIsPlacedOnASystemItSupports`, `testAModuleIsPlacedOnASystemItDoesNotSupport`, `testAModuleDeclaresNoPlatforms`
- [x] 7.5 Record the placed entry's platform record and `serviceManager` and add both to `keyInput`; verify a case asserts one service placed on two machines of different systems produces two entries with different records and different keys, and that an unplaced entry records neither
  - `testOneServicePlacedOnTwoSystems`, `testAnUnplacedMemberRecordsNoTarget`, `testAMachineChangesArchitecture`

## 8. The declared closure, its provenance and the store-path grammar

- [x] 8.1 Add `storeDir ? builtins.storeDir` to `mkPlan` (`lib/default.nix:83-89`) and thread it to the recogniser; verify a case asserts a deployment planned against a relocated store recognises paths under the given directory and does not recognise a path under the default one, and that no literal store directory remains in `lib/`
  - `testADeploymentPlannedAgainstARelocatedStore`, `testTheDefaultStoreDirectory`, `testAnAbsolutePathOutsideTheStoreDirectory`; `grep -rn '"/nix/store' lib/` is empty
- [x] 8.2 Rebuild `storePathsIn` (`lib/util.nix:74-80`) from the store directory plus Nix's store-path grammar — a 32-character hash over `0123456789abcdfghijklmnpqrsvwxyz`, a hyphen, a name; verify cases assert a genuine path is recognised, a 32-character run containing `e`, `o`, `t` or `u` in the hash position is not, a 31-character hash is not, and two paths in one string are both recognised
  - `testAStorePathIsRecognisedByItsGrammar` (all four cases in one table), `testARunOutsideTheAlphabetIsNotAHash`, `testTwoStorePathsInOneString`
- [x] 8.3 Read declared closure roots from the implementation, record them as the entry's `closure`, and keep them in `keyInput`; verify a case asserts the plan records exactly the declared roots as literal strings and that the closure names roots rather than paths inside them
  - `testAModuleDeclaresTwoRoots`, `testTheClosureNamesRootsAndNotPathsInsideThem`
- [x] 8.4 Demote the scan to a verifier over the whole entry — unit fields, unit environments, extension values, configuration-file fragments, generated-file paths and exported values — emitting `closure-path-undeclared` for an undeclared mention and a warning for a declared root nothing mentions; verify cases assert the error for a package named only by a configuration file and for one named only by an export value, the warning for an unmentioned root, and no rows for a consistent entry
  - `testAPackageInACommandIsNotDeclared`, `testAPackageNamedOnlyByAConfigurationFile`, `testAPackageNamedOnlyByAnExportValue`, `testADeclaredRootNothingMentions`, `testAConsistentEntry`
- [x] 8.5 Land the mana-side injection the next task depends on: extend the entrypoint scope at `~/Projects/mana/nix/importer.nix:114` with the node's `{ key = lockKey; locked = sourceEntry.locked; }` (both already in scope at `:85` and `:102`), so a module receives its pin the way it receives its packages; verify a fixture module reads `pin` from the injected scope and a plan built from it records the same `rev` and `narHash` as `lock.json`, and that no fixture hand-writes a pin
  - **landed in `~/Projects/mana`, uncommitted there.** That repository is a separate git tree, so this change's commits do not carry it; `git status` there shows exactly six paths - `nix/importer.nix`, `nix/importer-tests.nix`, `entrypoint.nix`, `tests/fixtures/dep-with-pin/mana.nix`, `tests/fixtures/dep-with-pin/pin-entrypoint.nix` and `tests/fixtures/nixpkgs-passthrough.nix` - all `nixfmt`-clean, with the importer suite passing.
  - `nix/importer.nix`: the node's `pin` is derived where the source is, above `fetchTree`, and `source = fetchTree pin.locked` now reads the same record - so `locked` is by construction the set the fetch was given: the source's identity without the mutable `ref`, and the locked fields without `lastModified`, `lastModifiedDate` and `shortRev`. Those three name no bytes, so a consumer keying on them would re-key without the source having moved. `importEntrypoint` applies `scope // { inherit pin; }`, so a dependency's entrypoint receives its pin the way it receives its packages.
  - `nix/importer-tests.nix::test_entrypoint-receives-its-own-pin` asserts the injection against `~/Projects/mana/lock.json` itself rather than a hand-written copy of its shape: the fixture dep carries a `mana.nix` (which is what makes the importer apply an entrypoint instead of importing `default.nix` raw), `fetchTree` is mocked to bytes only, and every expected field is named one by one - `type`, `owner`, `repo`, `rev`, `narHash` read out of the lock - so a leak of `ref` or of the three no-byte fields fails the case. Both importer cases pass: `nix eval --json '.#packages.x86_64-linux.tests.importer'` in `~/Projects/mana`.
  - The planner half, verified as a throwaway cross-repo probe (`/tmp/claude-outputs/pin-probe.nix`): the pin bytes mana's evaluation produced are handed to a module of the shape `{ pin, ... }: _: { inherit pin; ... }`, which hand-writes no revision, and `mkPlan` records `plan."svc:only@one".pin` equal to it with `diagnostics == [ ]`; `rev` and `narHash` equal `lock.json`'s `/nixpkgs` entry and no no-byte field appears. The entry key is a function of it, which `testAPinChangesAndTheStorePathsMove` already pins from the suite side.
  - `entrypoint.nix` now exposes `tests = import ./tests.nix` (both suites) instead of `libTests.nix` alone: the importer cases were unreachable from `default.nix`, so nothing outside an impure evaluation of the working tree could run them. Pre-existing and untouched: `tests.lib` re-exports `lib`, so the whole set is not JSON-serialisable (`cannot convert a function to JSON` at `nix/lib.nix:146`); `nix-unit -f default.nix tests` from `nix shell -f dev.nix shell` is the intended runner, and `--apply 'ts: builtins.removeAttrs ts [ "lib" ]'` is how to read the lib suite as JSON.
  - Found while doing it, not fixed and not in this change's scope: mana's importer never reads a dependency's own `mana.nix` `entrypoint`, and a consumer-named `entrypoint` on a dependency that has no `mana.nix` is silently ignored - both fall through to `default.nix` (`nix/importer.nix:146-160`). That is why the test fixture needs a manifest.
- [x] 8.6 Add `pin` to `moduleKeys` (`lib/module.nix:20-27`) as a key string and a locked record of literal strings (`type`, `owner`, `repo`, `rev`, `narHash` — the shape at `~/Projects/mana/lock.json:15-21`); record it on the placed entry and add it to `keyInput`; add no registry, no default and no per-instance override, because resolution belongs to `mana`; verify cases assert a locked record missing `rev` or missing `narHash` is an error row naming the module and the field with the entry still in the plan, that two members resolved to one key record equal pins and produce no row, that two members on different keys do not re-key each other, and that a changed pin re-keys exactly its own entry
  - `testAModuleDeclaresAFullySpecifiedPin`, `testAPinNamesNoRevision`, `testAPinNamesNoContentHash`, `testAPinWithNoLockKey`, `testEveryServiceResolvedToOneSource`, `testServicesResolvedToDifferentSources`, `testAPinChangesAndTheStorePathsMove`, `testThePlannerDoesNotVerifyAPin`
- [x] 8.7 Verify `tests/suites/perf.nix`'s `testTheHarnessRealisesNothing` still reports zero derivations, every store path a string, and at least one store path recorded
  - `nix eval --json .#planner.failuresBySuite` reports `perf: [ ]` with the fleet fixture rewritten to the new vocabulary

## 9. One regeneration and one re-pin

- [x] 9.1 Regenerate the golden plan with `python3 tests/regenerate.py`; verify `nix build .#checks.x86_64-linux.planner-pytest -L` passes and that the diff of `fixtures/minimal-typed-edge/plan/backup.json` shows only re-keyed entries, the new unit fields, the platform records, the new configuration-file records and the declared closures
  - `wrote fixtures/minimal-typed-edge/plan/backup.json: 8 entries, 7 prose fields kept`; +471/-16 lines, all of it keys, `target` platform records, `storeDir`, per-unit `env`, the new `configData` records and `closure` lists. The check is `planner-scenarios` in this repository (the task's `planner-pytest` name predates it) and it passes, together with `planner-tests`, `planner-python` and `planner-images`.
  - two scenario fixtures had to declare what the new readers require: `closure = [ postgresql ]` on both `postgresql/cluster.nix` impls, and `system`/`serviceManager` on both `deployment/machines.nix` registries. Without them the scenarios were `closure-path-undeclared` and `machine-target-incomplete` rather than clean plans.
- [x] 9.2 Re-measure with `bash perf/measure.sh`, re-pin `perf/budgets.json` in both directions with fresh provenance, and record which counters moved and by how much against 1.2's baseline; verify `nix build .#checks.x86_64-linux.planner-perf -L` passes without a budget being waived, and record separately what the memoised platform elaboration costs per architecture
  - re-pinned every fixture from `packages.planner-perf-results` at `date = "2026-09-05"`, interpreter 2.34.8, margin unchanged at 0.15. Every gated counter rose, none fell: `worked` per entry `nrThunks` 346.875 -> 1028.125 (x2.96), `nrFunctionCalls` x3.35, `nrPrimOpCalls` x3.08, `values.number` x3.00, `sets.bytes` x2.78, `envs.bytes` x3.29, `list.elements` x3.58, `gc.totalBytes` x2.69, and `nrOpUpdateValuesCopied` 4.875 -> 68.375 (x14.03), the typed readers' `//` over unit records, extension applications and the platform projection. `fleet-4` x2.4-x2.6 (`nrOpUpdateValuesCopied` x13.52), `fleet-256` x1.5-x2.1: the multiplier shrinks with fleet size because the added cost is per entry read, not per machine resolved. Growth against fleet size 4 is 0.42-0.52 at 256, still under the 1.25 bound. `nix build .#checks.x86_64-linux.planner-perf -L` passes with no budget waived.
  - the memoised elaboration, measured separately over 64 machines with 1, 2 and 4 distinct `system` strings (`nix eval .#lib --apply` under `NIX_SHOW_STATS`): 52632 / 53830 / 55943 thunks and 3209808 / 3272432 / 3388256 bytes GC, so about **1100 thunks and 59 KB per additional architecture**. The same three-architecture delta at fleet size 8 is +3199 thunks / +180112 bytes against +3311 / +178448 at size 64, which is the memoisation being per distinct architecture rather than per machine.

## 10. The image builder

- [x] 10.1 Add the builder as a package outside `lib/`, taking a plan and one placed entry key and wrapping `pkgs.portableService` (`nixpkgs:pkgs/build-support/portable-service/default.nix`); verify it fails naming the entry and the field when the entry records no platform record, and that it reads no file and no attribute outside the plan
  - `image/`: `read.nix` is the pure half, a function of the plan alone; `default.nix` the derivation half; `check.nix` the artifact the check asserts against. `suites/image.nix::testAFactThePlanDoesNotCarry` reads an entry whose `target` carries no platform record and gets the refusal naming the entry and the field; `testAnEntryWithNoUnit` is the other end of the same rule.
- [x] 10.2 Render one unit file per unit from the portable vocabulary, expressing only recorded fields, the per-unit environment, and `after`/`requires` as distinct relations; verify a rendered apply-and-exit unit carries the recorded timeout and remain-after-exit and no restart policy, and that an ordered-not-required unit does not pull in the unit it is ordered against
  - `testAnApplyAndExitUnit`, `testTwoUnitsWithDifferentEnvironments`, `testAnOrderingIsNotARequirement`, `testAScheduledUnit`; `python/test_image.py::test_a_unit_file_carries_what_the_entry_recorded` reads the file out of a built image and asserts it is the rendered text with `ExecStart=` and no `Restart=`.
- [x] 10.3 Render the systemd extension group into the unit file, failing the build naming the entry, the unit and the backend when an extension is recorded for a backend this builder does not implement, and naming the field when an extension field has no rendering; verify both failures fire and that no path produces a unit that merely lacks a field
  - `testASystemdExtensionIsRendered`, `testAnExtensionForAnotherBackend`, `testAnUnknownExtensionField`. The unknown-field refusal is a `subtractList` against `systemdDirectives`, so a field with no rendering cannot be silently dropped.
- [x] 10.4 Prefix every unit file name with the entry's instance and service and assert the prefix, satisfying `portableService`'s own assertion (`:90-92`); verify two entries of one instance placed on one machine, each declaring a unit of the same name, produce two non-colliding unit names
  - `testTwoServicesOfOneInstanceOnOneMachine` (`svc-first-daemon.service` beside `svc-second-daemon.service`), and `python/test_image.py::test_the_unit_files_are_prefixed_and_present` reads the names out of the built images.
- [x] 10.5 Populate the image from the entry's declared closure under the store directory the plan names, failing the build naming the entry, the unit and the path when a unit names a store path the closure does not contain; verify the failure fires on a hand-broken closure and that an entry planned against a relocated store populates that directory
  - `testAStorePathNamedButNotDeclared`, `testAMachineWhoseStoreDirectoryIsRelocated`; `python/test_image.py::test_a_command_resolves_inside_the_image` asserts every declared root is inside the squashfs under the plan's `storeDir`, and `::test_a_store_path_named_but_not_declared_fails_the_build` reads the refusal's sentence out of the evaluator's stderr.
- [x] 10.6 Copy a `source` configuration file out of the store to the host and assemble a `render` recipe on the host from its fragments and references, refusing to place either — or any secret byte — inside an image; verify the refusals fire, that assembly needs neither an evaluator nor the Nix daemon, and that the attachment shows both at the paths the plan records
  - `testASecretIsReferencedNotCarried`, `testARenderRecipeIsAssembledOnTheHost`; `python/test_image.py::test_a_render_recipe_is_assembled_on_the_host` runs the attach script with `PATH` holding coreutils and two stubs and nothing else - no `nix`, no evaluator - and asserts both dispositions land at their recorded modes, `::test_no_secret_and_no_configuration_content_enters_an_image` greps the image bytes for the secret and the copied source and finds neither, and `::test_a_missing_reference_refuses_the_attachment` asserts a machine that has not generated its secret gets a refusal before anything is written.
- [x] 10.7 Emit the attachment description naming the units, the confinement profile and every host path shown to the image, and refuse an entry whose unit needs an access the stated profile denies; verify the description names no path the entry does not imply, that the profile is taken from input rather than inferred, and that a refused profile failure names the access
  - `testTheDescriptionIsReviewable`, `testAProfileDeniesWhatAUnitNeeds`, `testAProfileDeniesAHostFileOnlyRootMayRead`; `python/test_image.py::test_the_attachment_description_is_reviewable` reads `attachment.json` beside each built image, `::test_a_denied_profile_fails_the_build` asserts the refusal names the unit, the access and the profile and says it is not widened, and `::test_detaching_leaves_nothing_behind` asserts detaching removes the staging directory and leaves the host's secret untouched.
  - the profiles table is read out of systemd's own drop-ins rather than restated: `default`, `nonetwork` and `strict` carry `DynamicUser=yes` and `ProtectSystem=strict`, `trusted` applies no restriction beyond a private `/tmp`.
- [x] 10.8 Refuse attachment on a machine whose platform record or `serviceManager` differs from the one the image was built for, before any unit starts; verify both refusals fire and name the two values compared
  - `python/test_image.py::test_an_image_meets_the_wrong_architecture` runs an aarch64 image's attach script on this x86_64 machine and asserts the refusal names both systems and that neither stub was called, `::test_an_image_meets_a_different_service_manager` removes the `systemctl` stub so the script reads `unknown` and asserts both managers are named, and `::test_another_service_manager_fails_the_build` asserts an entry planned for launchd produces no systemd image at all.
- [x] 10.9 Add a check building the worked example's placed entries into images; verify building the same entry twice produces byte-identical output, that changing a configuration file's recipe leaves every image byte-identical while reloading the units the file names, and that changing a unit's timeout changes exactly that entry's image
  - `checks.planner-images`: `image/check.nix` builds the worked deployment's four placed entries with real `borgbackup` and `openssh` closures plus four comparison builds, and `tests/python/test_image.py` (19 cases) asserts over the bytes. `test_a_rebuild_is_identical`, `test_a_configuration_change_leaves_the_image_byte_identical` and `test_an_unrelated_edit_changes_no_other_image` are one store path in each pair; `test_a_unit_field_change_changes_the_image` is two.
  - the image's version had to stop being the entry's key for the byte-identity rule to hold: a key changes when a configuration file's content hash changes, and that content never enters the image, so `read.nix` digests what the image contains (identity, unit records, closure, `storeDir`, host paths) instead. `suites/image.nix::testARebuildIsIdentical` now asserts the key moves while the version does not.
  - two library changes fell out of building a plan from real packages rather than literal strings: `util.shortHash` hashes the text of a record (`hashString` refuses a string carrying context) and `util.uniqueStrings` keys its dedup set by the text while returning the string it was handed, so a closure root that came from a package beside the plan keeps the context a consumer needs to copy its bytes.

## 11. Corpus, docs and verification

- [x] 11.1 Amend `notes/clan-portable-services-design.md` §16.7 (`:2341-2346`) with the paragraph recording that a systemd portable-service image is a binding of the unit vocabulary and not the unit format, and that a backend's own fields arrive as typed extensions rather than as a passthrough; verify the section still reads as a resolution of the collision rather than a reversal, and that §4-P1's note at `:736-739` is consistent with it
  - the amendment at `:2371-2385` keeps §16.7's resolution and adds a second binding beside launchd's `lib.services.configure`: the unit format is still the modular service, `portablectl`'s image is a third thing, and a raw `serviceConfig` passthrough is refused by §16.6's own "compat surface kept minimal on purpose".
- [x] 11.2 Record in the corpus what this change deliberately did not answer: the three routes to per-microarchitecture instantiation and their collision with §16.2's lexical-closure discipline (`:2230-2263`), written where a reader of §16.2 will find it; verify the note names the closure-sharing cost and does not claim a route was chosen
  - `:2272-2294`, inside §16.2 where the pin discipline is stated. All three routes are named with their own cost, the shared cost is spelled out as closure sharing against `notes/unaddressed.md:583-587`, and the note ends "No route is chosen here" - what the facts buy first is `lib.systems.architectures.inferiors` as a containment check, not tuning.
- [x] 11.3 Update the implementation return shape in `docs/authoring.md` (`:156-160`), the planes table in `docs/plan.md` (`:139-149`), the `mkPlan` argument list in `docs/README.md` (`:127-137`), and add every new row id to `docs/diagnostics.md`; verify the documented shape matches what a module may write by re-reading the readers added in 3.1, 5.1 and 8.4
  - the return shape at `authoring.md:233` carries `units`, `configData`, `closure`, `provides` and `vars`; `plan.md`'s planes table now reads by where a value arrived; `README.md:142` documents `storeDir`. Every id is in `diagnostics.md` and the retired `unit-environment-conflict` is gone, which `suites/source.nix::testEveryRowIsInTheReference` and `::testEveryHeadingIsAccountedFor` assert against the library's own source rather than against this note.
- [x] 11.4 Document `pin` on the declaration surface in `docs/authoring.md` beside the other `moduleKeys`, stating that its value arrives from the resolver through the module's lexical closure the way its packages do and is not written by hand, and that the planner records it without verifying it; verify the section names the resolver as replaceable rather than naming `mana` as required, since the requirement is two string fields and no tool
  - `authoring.md:75` in the declaration table and `:165-200` for the section: the value arrives through the lexical closure, the planner records and verifies nothing, and the requirement is stated as two string fields (`rev`, `narHash`) with `pin-underspecified` and `pin-malformed` as the two rows. The resolver is named as whatever produced the lock entry rather than as a required tool.
- [x] 11.5 Add one `tests/mapping.nix` entry per new spec heading and cross-walk every scenario in this change's five spec files to the case or check that exercises it, recording any deliberate omission with its reason; verify `nix eval --json .#planner.failures` is `{}`, which is what `tests/suites/coverage.nix` fails on
  - all five spec files are mapped, 225 headings accounted for. The realiser spec's bytes-and-scripts half names `python/test_image.py::<case>` through the mapping's `external` form; `suites/coverage.nix` gained the ability to hold both kinds in one list, because a requirement whose scenarios are one of each could not be expressed before. One omission with a reason: "A secret would be baked in" - the vocabulary has no way to name a generated file's bytes in a closure, so the build the scenario describes cannot be written to assert against.
  - `nix eval --json .#planner.failuresBySuite` is empty for all thirteen suites.
- [x] 11.6 Run `openspec validate emit-systemd-portable-service-images --strict` and confirm it passes with the artifacts as implemented
  - `Change 'emit-systemd-portable-service-images' is valid`. One INFO: archiving would refuse the `planner/plan-artifact` delta because that spec exists only in the unarchived `implement-minimal-typed-edge` change, which is the condition the delta file itself records at its head.
- [x] 11.7 Run `nix build .#checks.x86_64-linux.planner-tests .#checks.x86_64-linux.planner-pytest .#checks.x86_64-linux.planner-perf -L` and the new image check, then `nix fmt`; verify all pass and that formatting touched only files this change edited
  - `planner-tests`, `planner-scenarios` (this repository's name for the pytest check), `planner-python`, `planner-perf` and `planner-images` all build. `nix fmt` reformatted 15 already-modified files and reports `0 changed` on a second run; it exits non-zero on `pkgs/qubasa-blog/posts/*.md`, where `vale` fails prose this change never touched - those files are unmodified in `git status`. Every check was rebuilt after formatting and passes.
