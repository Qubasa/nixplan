## 1. Registration while the change is in flight

- [x] 1.1 `git add openspec/changes/hold-declaration-shape-and-fold-set-reads/` so the flake can see the change at all, and add the three new spec paths to `excused` in `tests/unit/coverage.nix` with the precedent reason used for `generate-values-with-nixos-secrets` ("an unimplemented change: no task of hold-declaration-shape-and-fold-set-reads has been done, so nothing in this package claims to satisfy it yet"); verify `nix eval --json .#planner.suites.coverage.testEverySpecificationIsClassified.expr` reports empty `unclassified`, `vanished` and `unreadable`
- [x] 1.2 Confirm the tree is green before any library edit: verify `nix eval --json .#planner.failures` is `{}`

## 2. The fold in the library

- [x] 2.1 Add `fold ? null` to the `interface` constructor in `lib/interface.nix:47-54`, return it beside `name` and `exports`, and add `foldOf = iface: iface.fold or null`; verify a throwaway `nix eval --expr` builds an interface with and without a `fold` and that `nix eval --json .#planner.failuresBySuite.interfaces` is `{}`
- [x] 2.2 Add the malformed-fold check beside the existing atom checks in `lib/interface.nix`: a declared `fold` that is not a function is `interface-fold-not-a-function`, subject `interface.subjectOf reg iface`, message naming the interface, evidence stating that a fold is applied to a set keyed by provider entry, resolution naming the declaring file; verify a new `interfaces` suite test asserts the id, subject and severity
- [x] 2.3 Apply a declared fold in the `reach = "all"` branch of `lib/resolve.nix:960-969`: build today's `entryKey`-keyed attrset, then when `interface.foldOf` is non-null force `fold entryKeyed` inside `diag.guard` with the consuming entry key as subject and `what` naming the interface and the slot, taking the guarded value as the slot's value; verify `nix eval --json .#planner.worked.plan` still equals `fixtures/minimal-typed-edge/plan/backup.json` field for field Recorded: the fold decides `results.<slot>` only. `lib/plan.nix:204-226` builds the read record from `edge.value`, so the edge now returns `value` (the entry-keyed set, which the plan and the entry key keep) beside `implValue` (the folded value, which `mkMember`'s `results` hands `impl`). Without the split a folded read would put the fold's output into `reads.<slot>.entries` and break the record every requirement of the delta spec asserts.
- [x] 2.4 Make a raised fold undeliver the read: the guard's row is `interface-fold-raised`, and the slot is excluded from the values `impl` receives rather than delivered empty; verify a `resolution` suite test asserts the row and that the consuming entry's received slot set does not contain the slot name
- [x] 2.5 Emit `interface-fold-unapplied` as a **warning** for an interface that declares a fold when the deployment resolved no set-reach read of it, subject `interface.subjectOf`, derived from the reads already resolved rather than from a second traversal; verify a `resolution` suite test asserts one warning row for an unapplied fold, no row when one set-reach read exists, and `applicable = true` in both
- [x] 2.6 Confirm no raising call entered the library: verify `nix eval --json .#planner.failuresBySuite.diagnostics` is `{}`, which includes the source scan for `throw`, `abort`, `assert` and `check` under `lib/`

## 3. The slot-set observation

- [x] 3.1 In `compose.service` (`lib/compose.nix:20-47`), read the module a second time against the member's own `defaults` **only** when `settings.sources` reports at least one knob whose source is the deployment, forcing it under `diag.guard` and discarding that guard's rows; verify a throwaway `nix eval --expr` over a member the deployment did not configure shows the second read is not taken Recorded, with one correction: the baseline is `defaults // fixed`, the member's own values, rather than `defaults` alone. A knob a module declares fixed and does not default would raise under bare defaults, and only against the member's own values is the conditional skip exact - with no deployment knob `settings.values` is that value by construction. Measured with `builtins.trace` in the module: one application when the deployment configured nothing, two when it wrote a knob. Measured after the fact against D7's own wording, now that the authoring worktree is readable: with bare `defaults`, a member whose `uses` reads a knob the module declares `fixed` and does not default makes `mkPlan` abort with `attribute 'offsite' missing` - uncatchable, so the library stops being total. With `defaults // fixed` the same deployment returns rows. No in-tree fixture reaches it, because only the slot keyset is forced, so the tree is green either way; the deviation buys totality for a shape a module may legally write. Recorded in CLAUDE.md.
- [x] 3.2 Compare the `uses` keyset of the two readings and return a `slot-set-settings-derived` **warning** row per differing member: subject the member's module file, message naming the member and the differing slots, evidence quoting `excluded.constructs.enable.trigger` verbatim, resolution stating that a slot declared unconditionally and branched inside `impl` leaves a wire the planner can refuse; verify a `composition` suite test asserts id, severity, subject and both name lists Recorded: `compose.service` returns the observation as `slotSet` and `compose.slotSetRows` builds the row, which `mkMember` calls with `moduleSubject` - `service` knows no subject, and this is the shape `resolveSettings` and `namespaceRows` already have.
- [x] 3.3 Assert the three non-cases in the `composition` suite: a `provides` keyset derived from settings, a claim's `fixed` derived from a setting value, and a member whose every value came from its own defaults each produce no `slot-set-settings-derived` row; verify each test fails when the comparison is widened to `provides` or made unconditional Recorded: the widening mutation fails `testACapabilitySetDerivedFromSettingsIsNotReportedHere`, `testADeploymentDecidesTheCapabilitySet` and `testAWireNamesACapabilityTheDeploymentAdded`, and suppressing the row fails `testAKnobAddsASlot` and `testAKnobRemovesASlot`. Making the comparison unconditional fails nothing and cannot: with no deployment knob the two readings are one value, so only the cost changes. That half is measured by the trace count in 3.1 instead.
- [x] 3.4 Verify no in-tree deployment gains a row: `nix eval --json .#planner.worked.diagnostics` still renders equal to `fixtures/minimal-typed-edge/plan/diagnostics.txt`, and `nix eval --json .#planner.failures` is `{}`

## 4. The refusal rule

- [x] 4.1 Extend the resolution text of `implementation-unknown-key` (`lib/resolve.nix:714-722`) to name the interface fold as where a refusal of another module's value belongs, leaving its id, subject and severity unchanged; verify a `resolution` suite test asserts the unchanged id and the new resolution text
- [x] 4.2 Add the `diagnostics` suite tests for the refusal-origin requirement: a declaration carrying `severity` still produces the existing warning and retags nothing; an `impl` returning a `refusals` key produces `implementation-unknown-key`; a fold's refusal carries the planner's id, subject and severity with the author's own message; two consumers of one refused provider produce two rows with different subjects and neither is deduplicated; a module raising outside a fold still produces `module-raised`; verify `nix eval --json .#planner.failuresBySuite.diagnostics` is `{}` Recorded: the spec's "the row SHALL carry the fold author's own message" needs a fifth row id. `builtins.tryEval` reports that something raised and never what it said, so a refusal is a returned value: a fold returning `{ refused = "<why>"; }` is `interface-fold-refused`, with the fold's message and the planner's id, subject and severity. A raising fold stays `interface-fold-raised` with the planner's text.
- [x] 4.3 Update any full row-id inventory or row-count assertion in `tests/unit/diagnostics.nix` for the four new ids; verify the suite passes and each asserted count matches the ids the library can now emit Recorded: nothing to change. The only inventory in `tests/unit/diagnostics.nix` is `testEveryAuthoringMistakeAtOnce`, which enumerates the rows of its own fixture, and that fixture declares no fold and no settings-derived slot set.

## 5. Scenario coverage

- [x] 5.1 Move the three spec paths from `excused` to `accountable` in `tests/unit/coverage.nix`; verify `nix eval --json .#planner.failuresBySuite.coverage` now names the unmapped scenario headings rather than the unclassified files
- [x] 5.2 Map all eleven `planner/interface-fold` scenarios to the `interfaces` and `resolution` test names from tasks 2.1-2.5, adding any test the mapping reports missing; verify the coverage suite reports nothing outstanding for that spec Recorded: ten of the eleven are `resolution` tests and `testADeclaredFoldIsNotAFunction` is an `interfaces` test, including the delivery-set scenario, which builds its own generator-backed deployment in `resolution` rather than reaching into the secrets suite.
- [x] 5.3 Map the five `planner/typed-edge` scenarios to the `composition` tests from tasks 3.2-3.3; verify the coverage suite reports nothing outstanding for that spec
- [x] 5.4 Map the five `planner/diagnostics` scenarios to the tests from task 4.2; verify `nix eval --json .#planner.failuresBySuite.coverage` is `{}`

## 6. Documentation

- [x] 6.1 Document `fold` in `docs/authoring.md` beside the `reach` section (`docs/authoring.md:119-155`): the field, that it applies to `reach = "all"` only, that its input is keyed by provider entry key and limited to the slot's `reads`, and that a raised fold leaves the slot absent; verify the interface table and the `reach` prose agree with `lib/interface.nix` and `lib/resolve.nix`
- [x] 6.2 Add a "where a refusal lives" section to `docs/authoring.md` stating the rule from design D8 and pointing an author who wants to refuse a provider at the fold; verify no other section of that file still implies a module may tag a row
- [x] 6.3 Add the four new rows to `docs/diagnostics.md` in their sections with their message, evidence and resolution shapes; verify every id the library can emit appears exactly once in that file
- [x] 6.4 Record the two new invariants in `CLAUDE.md` under "Interfaces, composition, reads" and "Diagnostics": an interface may own the guarded fold of a set-valued read, and that fold is the only channel through which a module may refuse another module's value; verify the file still names every registration point it names today
- [x] 6.5 Update the published-surface row in `docs/README.md` if the `interface` constructor's arguments are spelled out there; verify the described arguments match `lib/interface.nix`
- [x] 6.6 Confirm no in-tree file gained a path from another repository: verify `nix eval --json .#planner.failuresBySuite.layers` is `{}`, which includes the scan asserting that every path written in any file, comments included, resolves

## 7. Budgets and verification

- [x] 7.1 Run `nix build .#checks.x86_64-linux.planner-perf -L` as a supervised process, take the replacement figures from the ratchet failure message, and write them into `perf/budgets.json` with the interpreter version and today's date, recording the measured direction of each of the nine counters rather than assuming the conditional second read is free; verify the check passes on a rerun Recorded: all nine gated counters rose, by 0.05% at fleet-256 and mesh-256 and by at most 2.2% at the smallest fixtures, so every figure is re-pinned with interpreter 2.34.8 and 2026-09-09. The rise is the second reading of every configured member, the `fold` field on every interface value and the bound entry-keyed set of every set-valued read.
- [x] 7.2 Reproduce both probes from proposal.md as throwaway deployments and delete them afterwards: a module whose `uses` is derived from a knob, asserting the warning row and that the plan stays applicable; and two consumers of one interface whose fold refuses a provider, asserting two rows with different subjects and both slots absent from the values each `impl` received Recorded, both run through `nix eval .#lib --apply` and nothing written into the tree. The knob probe: `["slot-set-settings-derived"]` with the plan applicable, against `["slot-unwired"]` for the same module the deployment left alone. The refusal probe: two rows, `first:only` and `second:only`, both error, both carrying the fold's own message, `SLOTS` empty in each consumer's unit and `reads.far.delivered` false on both sides.
- [x] 7.3 Run `nix build .#checks.x86_64-linux.planner-tests -L` and `nix build .#checks.x86_64-linux.planner-delivery -L` as supervised processes and confirm both pass; verify `nix eval --json .#planner.failures` is `{}` Recorded: both checks pass, `planner-perf` and `planner-perf-checker` with them, and `.#planner.failures` is `[]`. The machine layer was run too and is unaffected: `nix run .#planner-e2e` is 47 passed in 123s, the same 47 as before the change. One earlier whole-layer run died on the host rather than on the plan - rookery's second VM refused to start with `vhost-vsock: unable to set guest cid: Address already in use` - and both the folder alone (30 passed) and the whole layer passed on the rerun.
- [x] 7.4 Run `nix fmt` and confirm the only changes are formatting of files this change touched, and that `fixtures/**` is untouched
