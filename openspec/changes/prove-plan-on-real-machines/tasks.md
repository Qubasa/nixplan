## 1. The planner hands over the machine

- [x] 1.1 Capture the before-state: run `bash perf/measure.sh` and record in this task the counters it reports for each fixture, so the cost of 1.2 is a difference rather than a claim
  - Run as `nix build .#packages.x86_64-linux.planner-perf-results`, which is `measure.sh` with the flake's own korora and nixpkgs. nix 2.34.8, 2 repeats. Per plan entry, `runs[0]`, the aggregation `perf/check.py` gates on:
  - `worked` (8 entries): nrThunks 844.25, nrFunctionCalls 474.375, nrPrimOpCalls 317.625, values.number 1059.875, sets.bytes 14613, envs.bytes 10526, list.elements 209.75, nrOpUpdateValuesCopied 67.375, gc.totalBytes 53932
  - `fleet-4` (9): 762.2223 / 418 / 291.6667 / 938.4445 / 12931.5556 / 9208 / 186.2223 / 45.4445 / 46343.1112
  - `fleet-16` (33): 459.5152 / 288.9091 / 203.9091 / 559.9394 / 7534.5455 / 5903.2728 / 135.8788 / 16.1516 / 28325.8182
  - `fleet-64` (129): 375.0388 / 252.8838 / 179.4187 / 454.3101 / 6026.4187 / 4981.0233 / 121.8295 / 7.8528 / 22689.6125
  - `fleet-256` (513): 353.3023 / 243.6140 / 173.1170 / 427.1307 / 5638.3627 / 4743.7193 / 118.2144 / 5.7174 / 21247.6259
  - `mesh-4` (9): 827.4445 / 473.4445 / 343.1112 / 1032 / 13928.8889 / 10217.7778 / 228 / 45.7778 / 51390.2223
  - `mesh-16` (33): 531.1213 / 350.6364 / 261.2425 / 663.2728 / 8656 / 7017.4546 / 182.1819 / 16.6061 / 33546.182
  - `mesh-64` (129): 448.4264 / 316.3024 / 238.3643 / 560.3721 / 7182.5117 / 6123.3489 / 169.3954 / 8.3412 / 28143.3799
  - `mesh-256` (513): 427.1482 / 307.4678 / 232.4776 / 533.8948 / 6803.3685 / 5893.2866 / 166.1053 / 6.2145 / 26740.772
- [x] 1.2 Add `address` to `targetOf` in `lib/resolve.nix` so the machine value an implementation receives is `{ system, serviceManager, address }`, each field present only when the machine declared it; verify a `resolution` suite case asserts the field is present for a machine with an address, absent for one without, and that a module dereferencing an absent address produces a row naming the entry and the machine
  - `resolve.nix:206-227`. `resolution` suite: `testAServicePublishesItsOwnEndpoint` (the worked server renders `target.address` and the deployment declares it once), `testAMachineWithNoAddress` (present for `one`, absent for `bare`, and the raise a module writes when the field is missing is a `module-raised` row whose subject is `svc:only@bare`), `testTheAddressAConsumerReadsIsTheProducersNotItsOwn`.
  - Open point recorded rather than absorbed: an **unguarded** `target.address` on a machine with none is a missing attribute, and `lib/diagnostics.nix:56-58` plus `docs/diagnostics.md:170-181` document that as propagating rather than contained, so it produces no row at all. The suite case therefore reads the field the way a module can and raises, which is catchable and is a row. The delta spec's "an implementation that reads one SHALL produce a row" is only satisfiable in that catchable form.
- [x] 1.3 Verify the address is a key input: a `plan` suite case asserts that declaring one machine's address differently changes the keys of the entries placed on it, leaves every other entry's key unchanged, and leaves the placements, allocations and wires identical
  - `plan` suite `testAnAddressChanges`: `one.example:22` against `10.0.0.10`, with a wire whose exported value is a constant so the consumer on `two` is not re-keyed by a value it read.
- [x] 1.4 Regenerate the worked deployment's committed plan and diagnostics (`fixtures/minimal-typed-edge/plan/`) and any golden fixture the new key input moves; verify `nix eval --json .#planner.failuresBySuite` is empty and that the regenerated files differ only in keys and in nothing else
  - `python3 tests/regenerate.py`; `failuresBySuite` is empty for all 15 suites and `checks.planner-scenarios` (the golden comparison) passes. `plan/diagnostics.txt` is unchanged: it carries no key and no address, and the two rows it attributes to the folder are byte-identical.
  - The diff is 40 lines and is **not** keys alone. It is keys, plus the three things 1.2 and 1.6 add or remove: every placed entry's `target` gains `address`; the four registry addresses lose their `:22` (see 1.6); and `vault-repo:server@vault.settings.server.host` is gone. No URL, closure, unit or allocation moved - `ssh://borg@vault.example:22/srv/borg` is byte-identical before and after.
- [x] 1.5 Re-run `bash perf/measure.sh`, record the delta against 1.1 in this task, and run `nix build .#checks.x86_64-linux.planner-perf -L`; if the gate fails, move only the counters it names in `perf/budgets.json` and quote the measurement next to them, otherwise leave the budgets untouched
  - Delta against 1.1, per plan entry: `nrFunctionCalls`, `nrPrimOpCalls` and `list.elements` did not move on any fixture. `nrThunks` +0.04% to +0.14%, `values.number` -0.01% to +0.12%, `sets.bytes` +0.18% to +0.71%, `envs.bytes` +0.01% to +0.17%, `gc.totalBytes` +0.00% to +0.74%, `nrOpUpdateValuesCopied` +0.74% to +8.73% (the `//` the target record gained). Largest single move: `nrOpUpdateValuesCopied` of `fleet-256`, 5.7174 -> 6.2164.
  - The gate failed on 52 counters, every one of them because the committed budget was the previous measurement exactly (0.0% of headroom). Only those 52 were moved, each to the measured value rounded up at the fourth decimal, and each fixture that moved carries a `note` quoting its measurement and the largest move. `RawBudgetEntry` in `perf/check.py` gained the `note` field. `checks.planner-perf`, `planner-perf-checker` and `planner-python` pass.
- [x] 1.6 Remove the address from the settings of any fixture that only carried it to work around this gap, if one exists in the repository, and verify its suite still passes
  - `fixtures/minimal-typed-edge/modules/borg-repo/default.nix` carried `defaults.host = "vault.example"` and `server.nix` rendered `settings.host` into the `url` export. The default is gone and the export is rendered from `target.address`.
  - Entailed by that removal, and reported rather than absorbed: the registry's addresses were `<host>:22` while the URL appended `alloc.ports.ssh` separately, so `target.address` could only be substituted if the address is the host alone. The four addresses in `deployment/machines.nix` are now bare hostnames, which is also the shape the cluster fixture of task 4.1 needs (`http://<address>:<allocated port>/`). The published URL is unchanged; `composition` suite `testTheFoldersRootsEvaluateUnmodified` lost the `host` knob from its expectation.

## 2. Seams, probed before anything is built on them

- [x] 2.1 Probe the delivery seam by hand, outside any test: boot one rookery VM from its own base image, `nix copy --to "ssh://root@$(ip)" --no-check-sigs` a small store path through `Cluster.run`, and verify on the guest that the path is valid (`nix-store --check-validity`); record the exact command, the `NIX_SSHOPTS` used and the wall time in this task
  - Probed with two VMs from `/nix/store/m1892c9yaw6ybqwki5kz4wqrlzi7mwsy-rookery-base-image/nixos.qcow2`, `secure_boot=False`, offline, 2048 MiB each. The probe drives `cluster/delivery.py` rather than a paraphrase of it.
  - Command: `nix copy --to ssh://root@10.0.0.10 --no-check-sigs /nix/store/3v4q68k0acsrbj793ms0m9wwwfd790h8-cluster-page-first`, run through `Cluster.run` (which wraps it in `nsenter --preserve-credentials --user=... --net=...`).
  - `NIX_SSHOPTS`: `-F /dev/null -i <generated key> -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -o GlobalKnownHostsFile=/dev/null -o BatchMode=yes`.
  - Wall time: vsock sshd answering at 10.49s, DHCP leases at 10.67s, the copy itself 0.35s, 11.53s for the whole probe. The addresses the cluster leased were `10.0.0.10` and `10.0.0.11`, which are the two the fixture's registry declares.
  - The guest answers `VALID` to `nix-store --check-validity` for the delivered path, and `m1` - which was not delivered to - exits 1 for the same path, so the copy moved something rather than finding it already there.
  - **`-F /dev/null` is required and design.md did not state it.** Without it the copy fails with `Bad owner or permissions on /nix/store/...-libvirt-12.6.0/etc/ssh/ssh_config.d/30-libvirt-ssh-proxy.conf` and `error: failed to start SSH connection to '10.0.0.10'`: the copy runs in the cluster's single-uid user namespace, where a host config owned by real root appears owned by `nobody` and ssh refuses to read it. rookery's own control channel carries the same flag for the same reason (`rookery/qemu/access.py:52-56`). It is in `delivery.ssh_opts` with that reason written beside it, and pinned by a case in `test_cluster_offline.py`.
- [x] 2.2 Probe the environment seam: read a rookery build's `nix-support/propagated-build-inputs`, assemble `PATH` and `PYTHONPATH` from it, and verify `python3 -c "import rookery.qemu"` succeeds and `qemu-system-x86_64`, `dnsmasq`, `pasta`, `virtiofsd`, `swtpm` and `nsenter` all resolve; record the python version rookery was built for
  - `/nix/store/fsqyppmlz0imcnqgkv04gfsv30qj9954-rookery-0.1.0`, built from the local clone at `74468ac`. `import rookery.qemu` succeeds and resolves to that path's `lib/python3.13/site-packages`. **rookery is python 3.13** (3.13.13).
  - All eleven binaries resolve: `qemu-system-x86_64`, `qemu-img`, `dnsmasq`, `pasta`, `virtiofsd`, `swtpm`, `nsenter`, `ip`, `nft`, `ssh`, `sshpass`.
  - Refinement to design.md D4, measured rather than assumed: the assembly must be **transitive**. `iproute2`, `util-linux` and `openssh` appear in the direct list as their `-dev` outputs, which carry no `bin`; each propagates its real output one level down (`util-linux-2.42-bin`, `iproute2-7.0.0`, `openssh-10.3p1`). Taking `bin` of the direct entries alone loses `ip`, `nsenter` and `ssh`. The runner walks the `propagated-build-inputs` closure.
  - Second measurement with a consequence for 7.1: this repository's `pytestEnv` is `pkgs.python3` from `nixos-unstable`, which is **3.14.7**, against rookery's 3.13. Importing across that boundary is what D4 refuses, so the runner's pytest environment is built on the interpreter whose minor version matches rookery's, and the runner compares the two and refuses with both named when they diverge.
  - `nix build /home/lhebendanz/Projects/rookery#rookery` was red before this probe: two of its own unit tests assume host binaries its build sandbox has none of. Fixed at the source with the guard its siblings already use - `@needs_qemu_img` on `rookery/qemu/tests/test_disk.py:307`, and a skip for an absent `ssh` in `test_landlock.py:518`. 993 passed, 21 skipped.
- [x] 2.3 Record the measured host contract in this task: which of `/dev/kvm`, `/dev/net/tun`, `/dev/vhost-vsock`, the `vhost_vsock` module, `kvm` group membership and unprivileged user namespaces had to be present, the failure message each missing one produced, and the peak RSS and wall time of a two-VM boot
  - All five had to be present; every probe passes on this host once the devices are exposed. rookery's own `preflight()` order is `check_binaries`, `check_user_namespaces`, `check_kvm` (when `accel == "kvm"`), `check_vhost_vsock`, then the share and base-image checks; `verify_tun_device` fires later, before the first tap.
  - Messages, produced by pointing each probe at an absent device rather than by breaking the host:
    - `/dev/kvm`: `cannot open /dev/kvm (No such file or directory); confirm the host exposes /dev/kvm and the user is in the 'kvm' group` - so the device and `kvm` group membership are one message, not two.
    - `/dev/vhost-vsock`: `cannot open ... ; the SSH control plane reaches guests over vsock and needs this device. Load the host kernel module (boot.kernelModules = [ "vhost_vsock" ] on NixOS) and ensure the user may open it.`
    - `/dev/net/tun`: `cannot open ... ; the host must expose the tun device for tap networking`
    - Binaries: `required binaries not found on PATH: qemu-system-x86_64, qemu-img, pasta, dnsmasq, ip, nft, nsenter, setpriv` - eight, and `setpriv` is one the README's published list omits.
    - Unprivileged user namespaces: `cannot create an unprivileged user namespace with uid/gid maps. On NixOS enable 'security.allowUserNamespaces' ...`. Measured present here: `user.max_user_namespaces = 124477` and `unshare --user --map-root-user true` succeeds.
  - Two-VM boot, offline, 2048 MiB ceiling and 2 vCPUs each: vsock sshd answering at **10.87s**, both DHCP leases at **11.06s**, settled at 11.24s. **Peak QEMU RSS 1034.4 MiB across both processes** - about a quarter of the 4096 MiB committed, which is virtio free-page reporting handing idle guest pages back.
  - No NAT prerequisite was needed, which is D7's claim: the cluster is offline, so `check_nft_nat` is not run at all.

## 3. The guest image

- [x] 3.1 Add `cluster/image.nix`: a NixOS configuration carrying every rookery guest invariant with the assertion rookery carries for it (virtiofs in the initrd, `vmw_vsock_virtio_transport`, `nofail` on every non-root mount, networkd as the only DHCP client, systemd-boot on a GPT ESP, serial console on the primary UART, firewall off), plus `services.flakelets.enable = true` with no declared services; expose the qcow2 as `packages.planner-cluster-image`; verify it builds and that `nix eval` reports `services.flakelets.services` as `{ }`
  - Every invariant is restated as an `assertion` rather than a comment, so a future edit that drops one fails the build instead of producing a guest rookery cannot reach: the `virtiofs` initrd module, `vmw_vsock_virtio_transport`, `nofail` on `/rookery`, `networking.useNetworkd` with no other DHCP client, systemd-boot on the ESP, `console=ttyS0`, `firewall.enable = false`, `flakelets.enable` with an empty `services`, and no password authentication.
  - `nix build .#packages.x86_64-linux.planner-cluster-image` produces a 2.2 GB `nixos.qcow2`; `nix eval --json '.#packages.x86_64-linux.planner-cluster-image.guest.config.services.flakelets.services'` is `{}`. The package carries `.guest` and `.toplevel` beside the image so a caller can read the configuration the image was built from - `make-disk-image` builds inside `runInLinuxVM`, whose result carries no `config`.
  - One assertion is about the host rather than the guest: `systemd.services ? rookery-virtiofs-report`. The host reads share readiness off the serial log (`rookery/qemu/api.py:154-171`), so a guest that does not enumerate its virtiofs mounts there makes `Vm.wait_for_share` time out with nothing to say. That unit is what the first boot of this image was missing, and the timeout is what it cost.
- [x] 3.2 Add the key-seeding oneshot (design.md D6): install `/rookery/authorized_keys` into root's `authorized_keys` at boot, root-owned, and come up cleanly when the share is absent; verify by booting the image twice, once with a share carrying a generated public key and once without, that SSH as root succeeds in the first and is refused in the second
  - `cluster-authorized-key.service`, probed by hand with `/tmp/claude-outputs/probe_image.py` over the same image the suite boots:
    - with the share: ready in 13.11s, `stat -c '%U %a' /root/.ssh/authorized_keys` is `root 600`, the unit is `active`, an `ssh root@10.0.0.10 true` from the host namespace exits 0, and `flakelet-boot.service` and `flakelet-reconcile.service` are both `active`.
    - without any share: the guest still boots and binds its vsock sshd in 10.55s - that is what `nofail` on `/rookery` buys - and the same key is refused: `root@vsock-cid-3: Permission denied (publickey,keyboard-interactive)`.
- [x] 3.3 Verify the image knows nothing it will be told: assert with `nix path-info -r` that neither artifact's store path is in the image's closure, and that `flakelet-boot.service` and `flakelet-reconcile.service` are
  - The closure is 521 paths and holds neither `flakelet-site-server` nor `flakelet-check-client`; `$toplevel/etc/systemd/system` carries `flakelet-boot.service`, `flakelet-reconcile.service`, `flakelet-providers.target` and `flakelet.target`. The endpoint is in the image, the plan is not.

## 4. The two-machine deployment fixture

- [x] 4.1 Add `cluster/deployment/` - `machines.nix` naming `alpha` at `10.0.0.10` and `beta` at `10.0.0.11` with their systems and service managers, `interfaces/`, one producing module publishing `http://<its target's address>:<its allocated port>/` and one consuming module fetching what it reads from the wire - with the address appearing nowhere but the machine registry; verify `mkPlan` reports `applicable = true` with no error rows, a plan with exactly the two placed entries, and that `grep` finds the address in one file of the fixture
  - `machines.nix`, `instances.nix`, `interfaces/default.nix`, `modules/page/{default,server}.nix` (the producer), `modules/probe/{default,client}.nix` (the consumer), and `default.nix` as the only glue - the same shape `tests/scenarios/shared-postgres/` uses.
  - `applicable = true`, `diagnostics = [ ]`, and the plan's keys are exactly `site:server@alpha`, `check:client@beta`, `machine:alpha`, `machine:beta`. The consumer's unit command carries `http://10.0.0.10:8080/`, rendered by the producer from `target.address` and its allocated port; the consuming module names neither.
  - `grep -rn '10\.0\.0\.'` over the fixture matches `machines.nix` only. Placements select on the `serves` and `fetches` tags, so `instances.nix` names no machine either.
  - The producer's unit binds `0.0.0.0` rather than `target.address`: the unit starts before the guest's DHCP lease exists. What the plan is held to is the published URL, which the consumer dials.
- [x] 4.2 Build both entries through the existing flakelet builder as `packages.planner-cluster-artifacts`; verify each artifact's top level is exactly `units/` and `meta.json`, that the producer's unit binds the allocated port, and that the consumer's unit text names the producer's address and port
  - `cluster/artifacts.nix`, wired at `flake-module.nix:229-233` and `packages.planner-cluster-artifacts`. The link farm carries `site`, `check`, `plan.json`, and - for the redelivery requirement - `site-changed` plus `plan-changed.json`, the same deployment with a different served file.
  - Each of the three artifacts' top level is exactly `meta.json` and `units/`. `site/units/site-server-serve.service` has `ExecStart=.../python3 -m http.server 8080 --bind 0.0.0.0 --directory /nix/store/...-cluster-page-first`; `check/units/check-client-fetch.service` has `ExecStart=.../curl --fail --silent --show-error --retry 5 --retry-connrefused --retry-delay 2 --max-time 10 --output /run/cluster-probe.body http://10.0.0.10:8080/` with `Type=oneshot` and `RemainAfterExit=yes`.
  - The changed page moves the producer's key `sha256-5e6c6762ab2b383d` -> `sha256-52be162e7e1db102` and its `meta.json` `settings_hash` `56aa1f1fb0bf6bfa` -> `83c58186b64d52f1`, and leaves the consumer's key `sha256-512210991b127487` untouched.
  - The consumer's fetch carries `--retry 5 --retry-connrefused`, which is about boot ordering rather than flakiness: after both machines reboot there is no ordering between one machine's socket and another machine's client.
- [x] 4.3 Verify the plan and the bytes agree without a machine: a pytest case reads the address out of the plan, the URL out of the consumer's delivered unit and the port out of the producer's, and asserts all three describe the same endpoint
  - `tests/python/test_cluster_offline.py::test_the_plan_and_the_bytes_describe_one_endpoint`: the address from `machine:alpha`, the port from `site:server@alpha.alloc.ports.http` and the producer's `ExecStart`, the URL from the consumer's `ExecStart`. It also pins the plan's own record of the crossing: `provides.page.exports.url.readBy == [ "check:client@beta" ]` and the consumer's `reads.upstream.entry == "site:server@alpha"`.
  - The suite's session fixture is named `artifact` deliberately: `tests/python/conftest.py:173` has an autouse scenario-coverage fixture that depends on that name, and a suite whose subject is not the scenario corpus shadows it, as `test_flakelet.py` does.

## 5. The delivery driver

- [x] 5.1 Add the addressing and refusal half of the driver (`address_of(plan, key)` and the refusal): it reads the machine record's address out of the plan and refuses an entry the plan did not place on the machine asked for, naming the entry, the machine asked for and the machines it was placed on; verify with a pytest case over the plan JSON alone, with no cluster, that the refusal fires and names all three
  - `cluster/delivery.py`: `address_of`, `machine_of`, `machines_placed`, `placed_key` and `DeliveryError`. The address is read from the `machine:<name>` record rather than from the entry's target, so a delivery dials what the registry declared.
  - Cases in `test_cluster_offline.py`: the refusal names `site:server`, `beta` and `alpha`; a machine declaring no address is refused rather than dialled empty; `nix copy` argv is exact. `nix build .#checks.x86_64-linux.planner-cluster-offline` - 12 passed.
  - The module imports no rookery: the namespace and control handles are `Protocol`s, so the pure half type-checks and runs in a build sandbox with no VM.
- [x] 5.2 Add the delivery half: `nix copy --to "ssh://root@<plan address>" --no-check-sigs <artifact>` through `Cluster.run`, with `NIX_SSHOPTS` carrying the generated key and no host-key file, and a full environment because `Cluster.run` replaces it; verify the command exits 0 and that the artifact path is valid on the guest
  - `delivery.deliver` returns the address it dialled, so a caller asserts it was the plan's. `delivery_env` copies the caller's environment and adds `NIX_SSHOPTS` (`-F /dev/null -o UserKnownHostsFile=/dev/null -o StrictHostKeyChecking=no -o BatchMode=yes -i <key>`): `-F /dev/null` is load-bearing, not tidy - the copy runs inside the cluster's single-uid user namespace where a real-root-owned `/etc/ssh/ssh_config` is refused (`rookery/qemu/access.py:52-56`), which is what the 2.1 probe failed with before it was added.
  - Observed on real machines: `test_the_machine_holds_what_the_artifact_names` finds the artifact and every store path its units name valid on the receiving guest, and `test_the_plan_names_the_address` compares the dialled address against `plan["machine:alpha"].address`.
- [x] 5.3 Add the activation half: `flakelet activate <name> <path>` over the control channel; verify the endpoint reports the entry with the plan's key as `locked_url` and generation 1
  - `delivery.activate`/`status`/`rollback` run over the vsock control channel, with stderr merged into the endpoint's stdout so what the machine says about the activation is the evidence. `status` raises `DeliveryError` when the endpoint reports no entry under the name.
  - Observed: `test_no_evaluation_happens_on_the_machine` reads `locked_url == plan[key].key` and `generation == 1` from the guest's own `flakelet status --json`.

## 6. The suite, one test per requirement

- [x] 6.1 Add `tests/python/test_cluster.py` with a session-scoped two-VM cluster fixture (offline, `secure_boot=False`, generated key, virtiofs share, state root under the runner's temporary directory) and the phase order stated in the file header; cover "No test double is involved": each machine answers over its own sshd, `flakelet` resolves to a store path on each guest, the image carries no artifact of the plan, and the deployment declares the address once; verify the assertions pass and that nothing in the file imports `unittest.mock`
  - `nix run .#planner-cluster` - **18 passed in 59.25s**, two booted guests, no mock in the file (`grep -c unittest.mock` is 0; the only import that is not stdlib or pytest is the driver).
  - The state root is a short `mkdtemp` rather than pytest's `tmp_path_factory`: the socket rookery opens is `<state>/rookery/rookery-<pid>-<id>/vm-<i>/virtiofs-<tag>.sock`, and under `/tmp/pytest-of-lhebendanz/pytest-N/cluster-state0` that path is 108 bytes - one over the `AF_UNIX` limit - so virtiofsd exited during startup with only the socket path to say why. The runner hands the directory in and keeps it on failure.
  - `test_the_machines_are_not_told_the_answer` cost the change its one spec edit. The scenario asked for "no store path of the entries" on the image, and the guest answered `nix-store --check-validity /nix/store/wyhh…-curl-8.21.0-bin` with 0: the image's own nix needs the same curl build the consumer's unit runs. A store is shared by construction, so the requirement now reads "none of the store objects an entry contributes" - the artifact, its unit files, the payload - and the case asserts every path a unit names is valid **iff** it is in that machine's own `/run/current-system` closure. Pre-seeding the plan into the image still fails it.
- [x] 6.2 Cover "A delivery moves one entry's closure to the machine the plan placed it on": after delivery the artifact and every store path its units name are present and valid on the receiving machine, and the address dialled was the plan's; verify each case passes
  - `test_the_machine_holds_what_the_artifact_names`, `test_the_plan_names_the_address`, `test_an_entry_is_not_delivered_to_a_machine_it_was_not_placed_on` - the last one asks for `site:server@alpha` on `beta` and catches the driver's refusal, then confirms `beta`'s store still does not hold it.
- [x] 6.3 Cover "The receiving machine evaluates nothing and fetches nothing": the unit becomes active, the endpoint names the plan key, the activation journal shows a prebuilt artifact and no resolving, and a fetch from outside the cluster fails because the cluster is offline; verify each case passes
  - `test_the_unit_runs_from_the_delivered_directory`, `test_no_evaluation_happens_on_the_machine` (the activation output says `using prebuilt artifact` and never `resolving`), `test_no_store_but_the_machines_own_is_reachable` (a `nix path-info --store https://cache.nixos.org` on the guest fails: the cluster has no egress).
- [x] 6.4 Cover "A wire the planner resolved is traffic between two machines": the consumer's unit succeeds on `beta`, the address in its delivered unit equals the plan's address for `alpha` and the address `alpha` leased, a socket listens on the allocated port on `alpha` and not on `beta`, and stopping the producer makes the consumer's request fail naming that address; verify each case passes
  - `test_the_consumer_reaches_the_producer`, `test_the_address_used_is_the_address_the_plan_recorded` (plan record, delivered unit text and `ip -json addr` on `alpha` all give `10.0.0.10`), `test_neither_end_was_told_the_address_by_the_harness` (the string appears in no file of the deployment but `machines.nix`, and in no environment of either unit), `test_the_allocated_port_is_the_listening_port`, `test_cutting_the_wires_far_end_is_visible`.
- [x] 6.5 Cover "A redelivery decides by identity, and a machine can go back": an unchanged redelivery leaves the generation and the MainPID alone, a changed unit becomes a later generation with an observably different response, and a rollback restores the previous generation and the previous response; verify each case passes
  - `test_an_unchanged_entry_is_a_no_op` (generation and `MainPID` both unchanged), `test_a_changed_entry_is_a_new_generation` (generation 2, and the body the consumer fetches changes), `test_rollback_returns_the_previous_generation` (generation 1 and the first body again).
- [x] 6.6 Cover "What the machine keeps across a reboot and a reconcile": both machines reboot, both entries become active again without a second delivery, the wire is traffic again, and the host's reconcile pass leaves the hand-activated entries registered; verify each case passes
  - `test_a_reboot_brings_the_entries_back` reboots in-guest with `systemctl reboot` over the control channel (design.md's open question, resolved the honest way), waits for both machines again, and finds both entries active with no delivery in between; `test_a_reconcile_leaves_a_hand_activated_entry_alone` runs `flakelet-reconcile.service` and finds the registered entries and their generations unchanged.

## 7. Wiring

- [x] 7.1 Add `apps.planner-cluster`: a runner whose derivation bakes in the image, the artifacts, the deployment and the suite, which checks the host and names what is missing before starting anything, resolves rookery at run time from `$ROOKERY_FLAKE`, assembles `PATH` and `PYTHONPATH` from its propagated inputs, refuses with both versions named if the python versions differ, and passes its arguments through to pytest; verify `nix run .#planner-cluster` completes a full pass and that `nix run .#planner-cluster -- -k wire` runs one selection
  - `nix run .#planner-cluster` - 18 passed in 59.25s. `nix run .#planner-cluster -- -k wire -v` - 1 passed, 17 deselected in 31.48s.
  - `PATH` and `PYTHONPATH` come from rookery's `nix-support/propagated-build-inputs`, walked transitively, which is the same assembly the 2.2 probe measured. The runner prints the resolved rookery store path so a run says which build it used.
- [x] 7.2 Verify the runner's refusals without the prerequisites: with `ROOKERY_FLAKE` unset to something unfetchable, and on a host missing `/dev/vhost-vsock`, the runner stops naming the missing thing and starts no VM
  - `ROOKERY_FLAKE=/nonexistent/rookery nix run .#planner-cluster` - exit 1, banner `ROOKERY COULD NOT BE RESOLVED` quoting the value and nix's own `getting status of "/nonexistent/rookery": No such file or directory`; `pgrep -c qemu-system-x86_64` is 0 before and after.
  - The device half is checked without breaking the host: `host_problems` takes the device list as a parameter, so `host_problems((("/dev/rookery-absent", "the guest LAN needs it"),))` returns `['/dev/rookery-absent: No such file or directory - the guest LAN needs it']` and `host_problems()` on this host returns `[]`. The check runs before rookery is resolved, so a host missing a device never reaches a VM.
- [x] 7.3 Add `checks.planner-cluster-offline` (design.md D3): pytest over the built artifacts and the plan for every case that needs no machine - the addressing, the refusal, the three-way endpoint agreement and the artifacts' bytes; verify the check passes and that it is listed by `nix flake show`
  - `nix build .#checks.x86_64-linux.planner-cluster-offline` - 12 passed in 0.02s, inside the build sandbox with no VM and no network.
  - `nix eval --json .#checks.x86_64-linux --apply builtins.attrNames` lists it beside the others; `apps.x86_64-linux` is `["planner-cluster"]`.
- [x] 7.4 Add the new pytest files to `checks.planner-python`; verify `ruff format --check`, `ruff check` and `mypy --strict` pass over them with no ignore added
  - `checks.planner-python` copies `cluster/delivery.py` and `cluster/runner.py` beside `tests/python/*.py`, so the driver and the runner are linted and type-checked as this repository's own Python. `nix build .#checks.x86_64-linux.planner-python` passes; no `# type: ignore` and no `# noqa` was added.
  - The suite imports rookery through `importlib` rather than statically, because the module only exists once `PYTHONPATH` is assembled at run time; `mypy --strict` is satisfied without an ignore because the handle is typed `Any` at that one call.
- [x] 7.5 Add one `tests/mapping.nix` entry per heading of both specs of this change, cross-walking the cluster scenarios to their pytest cases in the external form and the plan-artifact scenarios to the `plan` and `resolution` suite cases, and register both spec directories in `tests/suites/coverage.nix`'s `specSets`; verify `nix eval --json .#planner.failuresBySuite` is empty
  - `clusterSpecRoot` is threaded through `flake-module.nix`, the written `planner-suite.nix`, `tests/default.nix` and into `coverage.nix`'s `specSets` as the change `prove-plan-on-real-machines` with `delivery/real-cluster/spec.md` and `planner/plan-artifact/spec.md`. The change-scoped key is what keeps this `plan-artifact/spec.md` from shadowing the two others named the same.
  - `nix eval --json .#planner.failuresBySuite` - every suite empty, coverage included.
  - The cross-walk is live rather than accepted silently: renaming one mapping key to `"Scenario: An address changed"` makes `coverage` report `testEveryHeadingHasAMappingEntry`, `testEveryHeadingIsAccountedFor` and `testNoMappingEntryNamesAMissingHeading`; reverting it returns the file byte-identical (`cmp`) and the suite to empty.
- [x] 7.6 Verify this change added no flake input and no `nixConfig`: `git diff` touches neither `flake.nix` nor `flake.lock`, and `nix flake metadata --json | jq -r '.locks.nodes | keys | join(" ")'` lists the same inputs as before
  - Inputs are `adios flake-parts flakelet korora nixpkgs root systems treefmt-nix` - the same list, and `grep -c rookery flake.nix flake.lock` is 0 in both: rookery is resolved at run time from `$ROOKERY_FLAKE` (design.md D2), so the app works without this flake being able to fetch it.
  - The staged `flake.nix`/`flake.lock` diff is the `flakelet` input of the unarchived `emit-flakelet-service-artifacts` change, whose own comment says so; this change's edits are confined to the repository root, `fixtures/minimal-typed-edge/` and its own `openspec/changes/` directory. `grep -rn nixConfig flake.nix pkgs/planner` is empty.

## 8. Documentation

- [x] 8.1 Add `docs/cluster.md`: the host contract as measured in 2.3, `ROOKERY_FLAKE` and why rookery is resolved at run time rather than pinned, what one run observes and in what order, how to attach to a live cluster while debugging (`rookery status`, `console`, `display`), which rookery file the guest invariants were copied from and how to diff it, and that the boot is outside `nix flake check` while everything else is not; verify every claim in it names the file, the command or the measurement it comes from
  - The host table is the 2.3 measurement verbatim - each row carries the message a probe produced on a host where that thing was missing, not a paraphrase. The numbers are the ones recorded there: 1034 MiB peak RSS across both QEMU processes, ~11s to SSH per machine, ~60s for a run.
  - It says why the boot is an app: a build sandbox has no `/dev/kvm`, `/dev/net/tun` or `/dev/vhost-vsock`, so what can be a check is `planner-cluster-offline` and what cannot is `nix run .#planner-cluster`. The default `$ROOKERY_FLAKE` quoted is the runner's own constant, `git+ssh://git@github.com/Qubasa/rookery`.
  - The attach section is rookery's own CLI as its README documents it (`status`, `ssh`, `console`, `display`, `down`), with the `--run-dir` escape for a different `$XDG_RUNTIME_DIR`, and the guest-invariant diff is two `grep`s that both produce output today (6 assertions in rookery's `nix/base-image-configuration.nix`, 13 in `cluster/image.nix`).
- [x] 8.2 Record the machine-value change where a module author reads about modules (`docs/` and the library's own reference table if it lists what an implementation receives); verify the table lists `address` beside `system` and `serviceManager`
  - `docs/authoring.md:212` `implArgs` table: `` `target` | `{ system, serviceManager, address }` `` with the absence rule and, at `:227-233`, why `target ? address` is the guarded read and why the field is in the target rather than beside it. The registry table at `:513` gains `address`. `docs/plan.md:60` says the same for the plan's own `target`, and `:33` shows it in a rendered entry.
- [x] 8.3 Point `docs/README.md` and the suite, check and app tables in `docs/tooling.md` at the new app, the new check and the new suite; verify the tables list every check and app the flake exposes
  - `docs/README.md` document index gains `cluster.md`. `docs/tooling.md` gains the app and the offline check in the run block, a `### The two-machine suite` section, and two tables listing every check `nix eval --json .#checks.x86_64-linux --apply builtins.attrNames` reports (`planner-tests`, `planner-scenarios`, `planner-images`, `planner-flakelet`, `planner-flakelet-vm`, `planner-cluster-offline`, `planner-perf`, `planner-perf-checker`, `planner-python`, `treefmt`) and the one app (`planner-cluster`), each with what it needs.

## 9. Verification

- [x] 9.1 Run `openspec validate prove-plan-on-real-machines --strict` and confirm it passes with the artifacts as implemented
  - `Change 'prove-plan-on-real-machines' is valid`.
- [x] 9.2 Run the existing check set (`planner-tests`, `planner-python`, `planner-images`, `planner-flakelet`, `planner-flakelet-vm`, `planner-perf`, `planner-perf-checker`, `planner-scenarios`) plus `planner-cluster-offline`, then `nix fmt`; verify all pass and that formatting touched only files this change edited
  - `planner-tests` 203/203, `planner-scenarios` 21 passed 63 skipped, `planner-images` 20 passed, `planner-flakelet` 12 passed, `planner-cluster-offline` 12 passed, `planner-perf` 0 failures 0 invalid comparisons, plus `planner-python`, `planner-perf-checker` and `planner-flakelet-vm` green.
  - `planner-scenarios` collects the whole `tests/python` directory, so it needed `cluster/delivery.py` copied beside the suites: without it the two cluster files are a collection error rather than a skip. Both then skip themselves, their artifact env var being unset there - the 63 skips against 33 before.
  - `nix fmt` changed three files, all of them this change's: `cluster/artifacts.nix`, `tests/mapping.nix`, `tests/suites/plan.nix`. Its prose linter still fails repository-wide on markdown this library does not own; `docs/cluster.md` carries 11 vale lints against 35-36 in each of its siblings, all of the same two classes (domain words the dictionary lacks, table cells starting lowercase). Every check was re-run after formatting.
- [x] 9.3 Prove the new assertions can fail: point the consuming module at the wrong machine's address and confirm only the wire cases fail; skip the delivery step and confirm only the closure cases fail; drop `address` from `targetOf` and confirm the `resolution` and `plan` cases fail; revert all three and confirm every file is byte-identical to before the mutation
  - **Wrong address** (`modules/probe/client.nix`, `${results.upstream.url}` replaced by a literal `http://10.0.0.11:8080/`): offline, exactly the two wire cases fail and 10 pass - `test_the_plan_and_the_bytes_describe_one_endpoint` and `test_the_consuming_machine_dials_the_producing_one`, each quoting `10.0.0.10` against the mutated command. On machines the failure is louder than the task predicted, and the reason is worth keeping: the consumer's unit is a oneshot whose success is its activation, so a wire that dials nothing makes `flakelet activate check-client` exit 1 on `beta`, the `delivered` session fixture error, and 16 cases error with 2 passing - the two that observe the machines before any delivery.
  - **No delivery** (the `deliver` call in the `delivered` fixture replaced by `address_of`, so the artifact is never copied): `flakelet activate site-server ... failed on alpha (exit 1)` - the endpoint cannot activate a store path its machine does not have - and again 2 passed, 16 errors. The closure cases are what would have failed had activation been reached; the machine refuses one step earlier than that.
  - **No address in the target** (`resolve.nix:227` `// (if decl ? address then ... )` replaced by `// { }`): `nix eval .#planner.failuresBySuite` does not report failures, it aborts - `error: attribute 'address' missing at fixtures/minimal-typed-edge/modules/borg-repo/server.nix:74:29`, the worked deployment's own publishing module, which is the refused-read contract rather than a catchable row. Per case: `resolution.testAServicePublishesItsOwnEndpoint`, `resolution.testAMachineWithNoAddress`, `resolution.testTheAddressAConsumerReadsIsTheProducersNotItsOwn` and `plan.testAnAddressChanges` all raise, while `platform.testAFullyDeclaredMachine` still passes; `planner-cluster-offline` fails at `modules/page/server.nix:38` for the same reason.
  - All three reverted from copies taken before each mutation and confirmed byte-identical with `cmp`: `lib/resolve.nix`, `cluster/deployment/modules/probe/client.nix`, `tests/python/test_cluster.py`. `nix eval --json .#planner.failuresBySuite` is empty again and the cluster suite is green on the reverted tree.
