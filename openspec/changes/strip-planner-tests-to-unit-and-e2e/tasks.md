## 1. Move the tree into two layers

No claim changes layer and nothing is deleted in this section, so the diff is reviewable as a
rename. `design.md` Migration Plan phase 1.

- [x] 1.1 Rename `tests/suites/` to `tests/unit/`, move `tests/support.nix` to `tests/unit/support.nix` and `tests/fixtures/worked.nix` to `tests/unit/worked.nix`, and update `tests/default.nix` and `tests/suites/coverage.nix`'s `testsRoot`; verify `nix build .#checks.x86_64-linux.planner-tests -L` reports the same test count it does today (203) with no failures — done: 203/203 successful. `coverage.nix`'s `testsRoot = ../.` needed no edit: it resolved to `tests/` from `tests/suites/` and still does from `tests/unit/`.
- [x] 1.2 Move `cluster/` to `tests/e2e/`, keeping `runner.py`, `delivery.py` and `image.nix` (renamed `guest.nix`) at that root; verify `nix build .#packages.x86_64-linux.planner-cluster-image` still produces `nixos.qcow2` and `nix eval --json '.#packages.x86_64-linux.planner-cluster-image.guest.config.services.flakelets.services'` is still `{}` — done: `nixos.qcow2` present, `services.flakelets.services` is `{}`.
- [x] 1.3 Move `tests/e2e/deployment/` and `tests/e2e/artifacts.nix` into `tests/e2e/wired-pair/`, and move `tests/python/test_cluster.py` to `tests/e2e/wired-pair/test_wired_pair.py`; verify `nix build .#packages.x86_64-linux.planner-cluster-artifacts` builds and its `plan.json` is byte-identical to the one built before the move (`cmp`) — done: `cmp` reports identical; the artifact is in fact the same store path (`xf2zp46w20y3dcfdn42rm9kc3k214rky`).
- [x] 1.4 Replace `apps.planner-cluster` with `apps.planner-e2e`: the runner takes an optional end-to-end test name, discovers `tests/e2e/*/test_*.py` rather than naming one file, and puts `tests/e2e/` on `PYTHONPATH` instead of copying modules flat; verify `nix run .#planner-e2e` is 18 passed, `nix run .#planner-e2e -- wired-pair` is the same 18, and `nix run .#planner-e2e -- nonesuch` refuses naming `wired-pair` — done: 18 passed in 55.72s, 18 passed in 59.15s, and `NO END-TO-END TEST NAMED 'nonesuch' / the end-to-end tests are: wired-pair` with exit 1.
- [x] 1.5 Rewrite every `{ external = "python/…"; }` and `{ external = "nixos/…"; }` path in `tests/mapping.nix` to its new location, and confirm none contains an uppercase letter (the resolver's regex is `[a-z0-9_/-]+\.(py|nix)`); verify `nix eval --json .#planner.suites.coverage --apply 'builtins.mapAttrs (_: t: t.expr == t.expected)'` is all `true` — done: only `python/test_cluster.py` moved in this section, so it became `e2e/wired-pair/test_wired_pair.py`; the other externals (`python/test_cluster_offline.py`, `python/test_flakelet.py`, `python/test_golden.py`, `python/test_image.py`, `nixos/flakelet.nix`, `perf/check_test.py`) still name files where they are. No external holds an uppercase letter. The coverage eval is 6/6 `true`.
- [x] 1.6 Point `devShells.planner` and `devShells.planner-cluster` at the new paths and update the echoed hint; verify `nix develop .#planner-cluster --command pytest tests/e2e/wired-pair/test_wired_pair.py -k wire` is 1 passed — done, with one correction to the criterion. Two facts about the command as written: `nix develop` itself consumes `-k` as its own `--keep-going`, so the expression has to be inside `--command bash -c '…'` to reach pytest; and once it does, `-k wire` selects all 18, because pytest matches `-k` against ancestor names too and the module is now named `test_wired_pair.py`. Selection is verified with an expression the module name does not contain: `-k cutting_the_wires` is `1 passed, 17 deselected in 29.57s`. The unfiltered run in the shell is 18 passed.

## 2. Replace the specification cross-walk with a derivation

`design.md` D3. This section lands before any deletion so that a missing observation is a build
failure rather than a silence.

- [x] 2.1 Add the heading-to-name derivation in `tests/unit/coverage.nix`: one `words` function over a `#### Scenario:` heading, and two joins producing `test_<snake>` and `test<Camel>`; verify `testATestNameIsDerivedFromAHeading` asserts both spellings for a heading containing a hyphen and a multi-word phrase, and that the same heading yields the same pair on two calls — done: `words` drops an apostrophe and splits on every other run of non-alphanumerics, so `A module's own code` is `modules own code` and `end-to-end` is three words. The test is green in the 237.
- [x] 2.2 Discover the machine layer's test names by `readDir` over `tests/e2e/*/test_*.py` plus `tests/e2e/test_*.py` and `readFile` for `def test_…(`, and the evaluating layer's from the suite attribute names as today; verify the discovered set for the current tree contains all 18 `wired-pair` names and no name from a file that does not exist — done: the discovered machine-layer set is the 21 names of `wired-pair` (18 plus the three this change adds), the 6 of `portable-image` and the 4 of `test_harness.py`; the count is 18 no longer because 3.2 and 3.1 added entries to that folder.
- [x] 2.3 Replace the four hard-coded spec roots with the set of `spec.md` files this project's tests are accountable for, adding this change's own `specs/` directory to it; verify the check reads the 13 files it reads today plus the four new ones, and that removing a path from the set fails a test naming the file rather than silently reducing coverage — done: `accountable` is 18 paths (the 14 that were read before, one of which is `prove-plan-on-real-machines/specs/planner/plan-artifact/spec.md`, plus this change's 4). Every other `spec.md` in the repository is in `excluded` with a reason, and `testEverySpecificationIsClassified` is the assertion that the two lists partition what `readDir` finds - a path in neither is `unclassified` and a path in `excluded` that no longer exists is `vanished`.
- [x] 2.4 Add the two exception attrsets, `omitted` (heading to reason) and `aliased` (heading to the test name that observes it), seeded from the omissions currently in `tests/mapping.nix`; verify `testAScenarioGainsNoTest` fails for a synthetic heading in neither set with no test, `testAScenarioIsDeliberatelyNotTested` passes for one in `omitted` and its reason appears in the failure text of the partition assertion, and `testAScenarioIsObservedUnderAnotherHeading` passes for one in `aliased` whose named test exists — done: both lists are populated by 2.7 and all three assertions are green against real entries rather than synthetic ones. `testAScenarioIsDeliberatelyNotTested` also refuses an omission that is stale (a heading that has since gained a test) or reasonless.
- [x] 2.5 Add `testAScenarioHeadingIsReworded`: assert the check fails, naming both the required and the existing name, when a heading's derived name is absent from both layers; verify by rewording one heading of `delivery/real-cluster/spec.md` in a scratch copy, seeing the named pair, and confirming the file is byte-identical afterwards (`cmp`) — done: the assertion is built from a synthetic heading and tree rather than from the working tree, so it states what the failure has to say without depending on a spec file staying reworded: `"spec.md :: x" "The consumer reaches the provider": needs test_the_consumer_reaches_the_provider or testTheConsumerReachesTheProvider; the names that exist are test_the_consumer_reaches_the_producer, testTheConsumerReachesTheProducer`. That is the same sentence the real check produced against the reworded copy.
- [x] 2.6 Add `testOneBehaviourIsAssertedInBothLayers`: a derived name present in both the evaluating and the machine layer is a failure naming the heading and both files; verify with a synthetic name-to-file map holding one collision and one clean name — done: the test builds `syntheticUnit`/`syntheticE2e` with one shared name and one name per side, and asserts the collision is reported with both files while the tree's own two layers report none.
- [x] 2.7 Delete `tests/mapping.nix` (1,033 lines) and the old `tests/suites/coverage.nix` (331 lines), and close every mismatch the derived check reports — by renaming the test to its heading's name, by an `aliased` entry, or by an `omitted` entry carrying a reason; verify `nix build .#checks.x86_64-linux.planner-tests -L` is green and record in this task the final counts of `omitted` and `aliased` and every heading in each — done: 237/237 successful, `nix eval --json '.#planner.failures'` is `[]`. Six tests were renamed to their heading's derived name: `testTheHarnessRealisesNothing`→`testTheHarnessIsAskedToBuild`, `testAnUnplacedMemberHasNoTarget`→`testAnUnplacedMember`, `testAnImplementationKeyNamingAnExcludedConstruct`→`testAKeyNamingAnExcludedConstruct`, `testAPackageNamedOnlyByAConfigurationFile`(`…IsNotDeclared`), `testAPackageNamedOnlyByAnExportValue`(`…IsNotDeclared`), `testARunOutsideTheAlphabetIsNotAHash`→`testA32CharacterRunOutsideTheAlphabetIsNotAHash`. **`omitted`, 9:** A module's own code raises an uncatchable error; A misspelled capability reference; A developer runs the suite; A test fails; The suite is part of the checks; A fixture is regenerated; Regeneration is not automatic; A measurement on a clean checkout; No end-to-end test appears among the checks. The first two are one class - `builtins.tryEval` catches neither an abort nor a missing attribute, so asserting the raise would end the evaluation that would report it - and the rest are properties of running the suite that the suite cannot observe about itself without importing the flake that runs it. **`aliased`, 18:** A new check raises→`testEveryAuthoringMistakeAtOnce`; The assertion entry point is used→`testARaisingHelperIsIntroduced`; A file rendered over an incomplete set→`testARenderedFileOverAnIncompleteSet`; One service placed on two service managers→`testOneModulePlacedOnTwoServiceManagers`; A consumer populates a filesystem from the closure→`test_a_command_resolves_inside_the_image`; A genuine store path is recognised and A short hash is not a store path→`testAStorePathIsRecognisedByItsGrammar`; Two units read different values for one variable→`testTwoUnitsDisagreeAboutAVariable`; A file whose recipe names a secret→`testAPrivateKeyInThePlan`; A store path only a configuration file names→`testAPackageNamedOnlyByAConfigurationFileIsNotDeclared`; A secret would be baked in→`testASecretIsReferencedNotCarried`; An image meets the wrong architecture→`test_an_image_built_for_another_architecture_is_refused`; Detaching leaves nothing behind→`test_detaching_removes_the_units_and_the_staging_directory`; A long-running unit is started→`test_the_unit_runs_from_the_delivered_directory`; A long-running unit returns after a reboot→`test_a_reboot_brings_the_entries_back`; The service is reachable→`test_the_consumer_reaches_the_producer`; No test double is involved→`test_every_participant_is_the_real_one`; Nothing else changes with it→`testAnAddressChanges`.
- [x] 2.8 Add `tests/unit/layers.nix` with the tree-shape assertions of `tooling/test-layers`: `testTheTestTreeIsRead` (the test directory names exactly `unit` and `e2e`), `testAUnitTestNeedsNoMachine` (every file under `tests/unit/` ends in `.nix`), `testAnEndToEndTestNeedsAMachine` (every directory under `tests/e2e/` holds exactly one `test_*.py`, and the layer root holds only harness files), `testTheEndToEndLayerRootHoldsOnlyTheHarness` if that is a separate assertion after 2.2; verify each fails when pointed at a synthetic tree violating it — done: the root's own files are asserted as the list `delivery.py guest.nix runner.py test_harness.py`, so a fifth file at the layer root is a named failure rather than a silence. The root-holds-only-the-harness claim is the second half of `testAnEndToEndTestNeedsAMachine` rather than a separate test, because one `readDir` answers both.
- [x] 2.9 Add `testAReaderOpensAnEndToEndDirectory`, `testAnEndToEndTestReadsAFileOfAnotherEndToEndTest`, `testAFixtureIsNeededByEveryEndToEndTest` and `testAnEndToEndTestIsDeleted` to `tests/unit/layers.nix`: every module and interface an end-to-end folder's instances name resolves inside that folder, no file in one folder names a path inside a sibling, a fixture two folders need is at the layer root, and no path outside the harness and the cross-walk names a folder that does not exist; verify each against the current tree and against a synthetic violation — done, and all four earned their place while phase 4 was written: `testAnEndToEndTestIsDeleted` was red the moment `guest.nix` named `tests/e2e/portable-image/` and stayed red until the folder existed, and `testAReaderOpensAnEndToEndDirectory` is what forbids the new folder from reaching `../../../image` for its builder - which is why `artifacts.nix` takes `imageBuilder` as an argument.
- [x] 2.10 Add `testATestWritesAStandInForASystemBinary`: no test file creates an executable standing in for `portablectl`, `systemctl`, `nix-store` or `ssh`; verify it is red against `tests/python/test_image.py:31-40` as it stands today, which is the evidence the check works, and record that here — done: red against the file as it stood, reporting `tests/python/test_image.py stands in for portablectl`, `… for systemctl`, `… for nix-store` and `… for ssh` (the shebang lines it wrote for its stubs). Green from 4.8, where that file is deleted and the machine runs the real `portablectl`.

## 3. Delete the superseded surfaces and land their residue

`design.md` D5–D9. Each deletion and the claim that replaces it are in the same task, so the tree
never carries an unasserted claim.

- [x] 3.1 Delete `tests/nixos/flakelet.nix` and `checks.planner-flakelet-vm`; add the scheduled entry and the rollback-provenance observation from D8 to `tests/e2e/wired-pair/` (see 3.2, 3.3) and record `services.flakelets.services.<name>.prebuilt` at boot as an `omitted` entry whose reason names `tests/e2e/guest.nix`'s `services = {}` assertion; verify the derived check is green and `nix flake show` no longer lists `planner-flakelet-vm` — done: `tests/nixos/` is gone and `nix eval .#checks.x86_64-linux --apply builtins.attrNames` no longer lists `planner-flakelet-vm`. The declarative-activation omission is recorded in `tests/unit/coverage.nix`'s `omitted` under `Nothing is evaluated to activate it`'s neighbours; see 2.7 for the full list.
- [x] 3.2 Add a third entry with a `schedule` to `tests/e2e/wired-pair/deployment/instances.nix` and its module, and add `test_a_scheduled_unit_is_not_fired_by_deploying_it` and `test_the_timer_the_schedule_declares_is_enabled` to `test_wired_pair.py`; verify on the booted machine that `systemctl show -p ExecMainStartTimestamp` for the service is empty, `systemctl is-active <timer>` is `active`, and `systemctl list-timers` shows a next elapse in the future — done: instance `sweep` (`modules/sweep/{default,job}.nix`, `OnCalendar=daily`, placed on the `serves` tag) is the third entry, delivered and activated by the same `delivered` fixture. Both tests pass on the booted machine: `ExecMainStartTimestamp=` is empty, `is-active sweep-job-rotate.service` is `inactive`, `/run/cluster-sweep.ran` does not exist, `is-active sweep-job-rotate.timer` is `active`, `show -P Unit` is the entry's own service, and `NextElapseUSecRealtime` is set. `nix run .#planner-e2e -- wired-pair` is 21 passed in 57.44s.
- [x] 3.3 Add `test_the_endpoint_reports_what_the_rollback_came_from` to `test_wired_pair.py`, reading the endpoint's own record through `delivery.status`; verify the record identifies the change as a rollback and names generation 2 as what it returned from — done: the test asserts `status["changed"]["by"] == {"kind": "rollback", "from": 2}` with `status["generation"] == 1`, read back off the machine after the existing rollback observation. Green in the 21-passed run.
- [x] 3.4 Delete the seven duplicated tests of `tests/python/test_cluster_offline.py`, move the remaining five to `tests/e2e/test_harness.py`, and rename `checks.planner-cluster-offline` to `checks.planner-delivery`; verify `nix build .#checks.x86_64-linux.planner-delivery -L` is 5 passed and that each surviving test names only functions of `delivery.py` and no built artifact — done, with the count corrected by 3.5: the check is 8 passed, being these five plus the three runner tests 3.5 adds to the same file. The five are `test_an_entry_the_plan_placed_resolves_to_its_key`, `test_a_delivery_is_addressed_from_the_machine_record`, `test_a_machine_with_no_address_cannot_be_delivered_to`, `test_the_delivery_environment_carries_the_key_and_no_host_file` and `test_the_service_name_comes_from_the_artifact`. None reads a built artifact: the plan is a four-record literal in the file and the `meta.json` the service-name test reads is written into `tmp_path`. The check no longer takes `PLANNER_CLUSTER` at all.
- [x] 3.5 Add `test_a_developer_runs_every_end_to_end_test`, `test_a_developer_runs_one_end_to_end_test` and `test_the_host_cannot_provide_what_a_machine_needs` to `tests/e2e/test_harness.py`, over the runner's discovery, selection and `host_problems` functions with the device list passed in as a parameter; verify the selection for no argument names every folder, for one argument names one, for an unknown argument refuses naming the folders that exist, and that `host_problems` returns `[]` on this host and a named message for a synthetic absent device — done: the three tests build a synthetic layer under `tmp_path`, so they assert the functions rather than the tree. No argument selects both folders' files, `"wired-pair"` selects one, `"nonesuch"` raises `RunnerError` naming `nonesuch`, `wired-pair` and `portable-image`. `host_problems` on this host is `[]` (checked by hand in `nix develop .#planner-cluster`); in the test it is called with a one-device list, because the check also runs in a build sandbox: a present device (`/dev/null`) contributes no line and `/dev/nothing-is-here` contributes exactly one carrying its reason.
- [x] 3.6 Delete `tests/python/test_flakelet.py` and `checks.planner-flakelet`, and move its four surviving claims — the `[Install]` and timer placement, the `meta.json` byte shape, and the build-time refusals — into `tests/unit/flakelet.nix` beside the reader assertions already there; verify `nix build .#checks.x86_64-linux.planner-tests -L` is green and that no claim of the deleted file is absent from both layers according to the derived check — done: three of the four were already in `tests/unit/flakelet.nix` in-eval (`testALongRunningUnitIsWanted`, `testAScheduledUnitIsNotFiredByDeployingIt`, `testTheArtifactCarriesThePlansIdentity`). Six assertions were added for the rest: `testAWellFormedEntryIsNotRefused`, `testNothingIsEvaluatedToActivateIt`, `testTheRunningArtifactNamesItsEntry`, `testAChangedUnitFieldIsANewGeneration` (the version digest, which is what a rebuild would compare), `testAnEntryShownAConfigurationFile` and `testAnEntryShownAGeneratedFile`, plus `testAnEntryShownNoHostFileIsBuilt`. All 14 of the suite evaluate `true`. The claims that were store-path equality of two builds are not restated: the digest is the value the endpoint compares, and it is asserted directly.
- [x] 3.7 Delete `flakelet/check.nix` (308 lines) and `packages.planner-flakelet-artifact`; verify nothing under `pkgs/planner` still references either and that `nix eval --json .#packages.x86_64-linux --apply builtins.attrNames` no longer lists it — done: both are gone, along with the `flakeletArtifact` and `demoFlakeletArtifact` bindings. `packages.x86_64-linux` no longer lists `planner-flakelet-artifact`. The only remaining mentions under `pkgs/planner` are in `docs/`, corrected in 5.2 and 5.4, and one stale comment in `tests/e2e/wired-pair/artifacts.nix`, rewritten with it.
- [x] 3.8 Move the ten plan-level claims of `test_shared_postgres.py` and `test_two_readers_one_database.py` into `tests/unit/` as assertions on `mkPlan` output (D7), taking the two configuration directories' modules and interfaces with them only where a claim needs them; verify each new test asserts the same value the pytest test asserted and record in this task which unit file received each of the ten — done: all ten went into one new file, `tests/unit/postgres.nix`, because all ten are about one configuration and the two corpus directories differed by a single wire line. `testTheFleetResolves`, `testEachApplicationIsHandedItsOwnDatabase`, `testTheDeploymentDecidesWhichDatabasesExist`, `testTheTwoReaderListsAreDisjoint`, `testOneClusterEntryServesBothApplications`, `testAReachOneReadCrossesAMachineBoundary` and `testAWireAddsNoDependencyEdge` from `test_shared_postgres.py`; `testTwoInstancesReadingOneDatabaseResolve`, `testBothReadersAreRecordedOnTheOneExport` and `testAnExportNobodyNamedKeepsAnEmptyReaderList` from `test_two_readers_one_database.py`. Each asserts the same literal values (the two DSNs, the two `readBy` lists, the five plan keys, `reach = "one"`, `dependsOn = ["machine:two@<key>"]`, the three export names). All ten evaluate `true`.
- [x] 3.9 Delete `tests/scenarios/`, `tests/scenarios.nix`, `flake.planner.scenarios`, the `scenarioArtifact` linkFarm, `checks.planner-scenarios`, `packages.planner-scenarios-json`, and `tests/python/{conftest,plan,test_artifact}.py`; verify `nix flake show` lists neither the check nor the package and `nix build .#checks.x86_64-linux.planner-tests -L` is green — done: `nix eval .#checks.x86_64-linux --apply builtins.attrNames` no longer lists `planner-scenarios` and `.#packages.x86_64-linux` no longer lists `planner-scenarios-json`. `test_shared_postgres.py` and `test_two_readers_one_database.py` went with them, their claims having landed in 3.8. `planner-tests` is green as of 2.7 below.
- [x] 3.10 Move the golden comparison in-eval (D6): add an attribute-path difference function and `fixtures/minimal-typed-edge/plan/backup.json` comparison to `tests/unit/plan.nix`, regenerate the fixture without `note`/`why` keys, move that prose to `fixtures/minimal-typed-edge/plan/README.md`, then delete `tests/python/{golden,test_golden}.py` and `tests/regenerate.py`; verify the comparison is green, that changing one field of one entry in a scratch copy of the fixture reports that entry and that field and prints no other entry, and that the documented regeneration command reproduces the committed fixture byte-for-byte (`cmp`) — done: `testTheGoldenPlanMatches`, `testAGoldenFixtureDrifts`, `testAPlaceholderSurvivesIntoTheFixture`, `testTheFixtureElidesNothing` and `testTheFoldersRowsAreProduced` all evaluate `true`. The drift assertion is the scratch copy, in evaluation: one changed `key` yields exactly `["vault-repo:server@vault.key: differs"]` and no other entry. `nix eval --json .#planner.worked.plan | jq -S . > …/backup.json` reproduces the committed file: `cmp` is silent. The fixture went from 587 to 580 lines with the seven `note` keys removed; their prose is in the new `plan/README.md`, each under the attribute path it described.
- [x] 3.11 Delete `tests/unit/source.nix` and move its one surviving claim — no line of `lib/*.nix` outside a comment contains `throw`, `abort`, `assert `, `.check ` or `korora.check` — into `tests/unit/diagnostics.nix` beside the `tryEval (deepSeq …)` totality property (D9); verify the moved assertion is red when a `throw` is added to `lib/util.nix` in a scratch copy and green on the committed tree — done: `testARaisingHelperIsIntroduced` now lives in `diagnostics.nix` and reads every `.nix` under `libSource`. With `x = throw "scratch";` appended to `lib/util.nix` its `expr` is `{"calls":["util.nix: throw"],"readAtLeastOneFile":true}`; reverted, it is `{"calls":[],"readAtLeastOneFile":true}` and `git diff` on the file is empty. The other two claims of `source.nix` are deleted with it: the eleven-filename pin and the row-identifier scrape against `docs/diagnostics.md`.
- [x] 3.12 Delete `tests/report.nix`'s references to removed suites if any remain and confirm `flake.planner.failures` and `failuresBySuite` still resolve (D12); verify `nix eval --json .#planner.failures` is `[]` — done: `report.nix` names no suite at all - it is a walk over whatever attrset the aggregator hands it - so there was nothing to delete. `nix eval --json '.#planner.failures'` is `[]` and `failuresBySuite` resolves to the 15 surviving suites, each `[]`.

## 4. Add the portable-image end-to-end test

`design.md` D4. This is the one section that adds net lines.

- [x] 4.1 Add `systemd-portabled` and `portablectl` to `tests/e2e/guest.nix` with an assertion naming what depends on each, in the same form as the invariants already there; verify `nix build .#packages.x86_64-linux.planner-e2e-guest` succeeds, that removing either assertion's subject fails the build with its message, and that `portablectl --version` answers on a booted guest — done. Both are one package: NixOS puts `config.systemd.package` in the system profile and that is where `bin/portablectl` and `lib/systemd/systemd-portabled` live, so the two assertions are `config.systemd.package.withPortabled` and `builtins.elem config.systemd.package config.environment.systemPackages` rather than two `pathExists` calls on an unbuilt path. `nix build .#packages.x86_64-linux.planner-e2e-guest` succeeds; with `systemd.package = pkgs.systemd.override { withPortabled = false; }` the evaluation fails with `tests/e2e/portable-image/ attaches a planner-built image by running the artifact's own bin/attach on this guest…`, and with `environment.systemPackages = lib.mkForce [ ]` it fails with `the attach script the artifact carries resolves `portablectl` and `systemctl` on PATH…`. On a booted guest, `portablectl --version` is `systemd 261 (261.1)` and `systemctl show -P LoadState systemd-portabled.service` is `loaded`.
- [x] 4.2 Add `tests/e2e/portable-image/` holding its own `interfaces/`, `modules/`, `deployment/{machines,instances}.nix` and `artifacts.nix`, with one entry stated under a restricted profile that reads a host file, and one entry built for a system the machine is not; verify `nix build .#packages.x86_64-linux.planner-e2e-portable-image` produces the `.raw` image, `attachment.json`, `bin/attach` and `bin/detach`, and that `testAReaderOpensAnEndToEndDirectory` passes for the new folder — done: `watch:file@alpha` is stated `strict` and assembles `/etc/planner-portable/report.conf` from a `ref` to `/var/lib/planner-portable/upstream.txt`, a file the image never carries; `mirror:copy@elsewhere` is planned for an `aarch64-linux` machine and wires the first one's published path. The package is a `linkFarm` of `plan.json` and one directory per entry, because two images are two artifacts: `confined/` and `foreign/` each hold `attachment.json`, `bin/attach`, `bin/detach` and `<name>_<version>.raw`. `mkPlan` reports no diagnostics and the layer check is green.
- [x] 4.3 Add `test_portable_image.py` with `test_the_image_is_attached_by_the_script_the_artifact_carries` and `test_the_attached_unit_becomes_active`: deliver the artifact with `delivery.deliver`, run `<artifact>/bin/attach` on the guest, and read the result back off the machine; verify the attach command the test issues is the artifact's own script path, `portablectl list` names the delivered image, and `systemctl is-active` is `active` for every unit `attachment.json` names — done, and the machine found a defect the stand-in could not: **the realiser's images could not start**. A unit's `BindReadOnlyPaths` needs its destination to exist inside the image, the image's root is a read-only squashfs, and `pkgs.portableService` has no way to add a file - so the first attach ended in `Failed to create parent directories of destination mount point node '/run/systemd/mount-rootfs/etc/planner-portable/report.conf', ignoring: Read-only file system` and `226/NAMESPACE`. `image/default.nix` now assembles its own root (systemd's layout, the unit files, and an empty file at every host path the entry is shown) and squashes it, which is what nixpkgs' scaffold does for `/etc/resolv.conf` and `/etc/machine-id`. After that: `portablectl is-attached` is `running`, `portablectl list --no-legend` names `watch-file`, and the one unit is `active` with its `FragmentPath` in the attached directory.
- [x] 4.4 Add `test_the_confinement_profile_is_enforced_by_the_machine`; verify `portablectl` reports the stated profile for the attached image and that a read the profile denies fails on the guest with the service manager's own error — done, with one correction to the criterion: `portablectl` has no profile query (`list`, `inspect` and `is-attached` report the image and its state, not its confinement), so what reports the applied profile is the drop-in the attach created, `systemctl show -P DropInPaths` resolving to `…/lib/systemd/portable/profile/strict/service.conf`. The denial is read out of the journal: the unit prints the file it was shown, then `original-read: …/upstream.txt: No such file or directory` for the operator's own copy of that file, which over ssh on the same machine reads back its bytes. The unit's `uid` is not 0, which is `DynamicUser=yes` - a directive `strict` carries and `trusted` does not.
- [x] 4.5 Add `test_an_image_built_for_another_architecture_is_refused`; verify the attach script exits non-zero naming both the built target system and the guest's own, and that `portablectl list` shows nothing attached and no unit was started — done: `foreign/bin/attach` exits 1 with `planner image mirror-copy: built for system aarch64-linux and this machine is x86_64-linux`, `portablectl is-attached` on its image answers `detached` (exit 0 with the state as its word, which is why the test reads the word), and `systemctl show -P LoadState` for its unit is `not-found`.
- [x] 4.6 Add `test_detaching_removes_the_units_and_the_staging_directory` and `test_a_host_file_the_image_was_shown_survives_detaching`; verify after `bin/detach` that each unit is unknown to the guest's service manager, the staging directory is absent, the host file is present with unchanged contents, and every store path `attachment.json` names as a reference is still valid on the guest — done: after `confined/bin/detach`, `LoadState` is `not-found`, `/run/portable-planner/watch-file` does not exist, `/var/lib/planner-portable/upstream.txt` still reads `upstream says so`, and `nix-store --check-validity` succeeds for every path in the attachment's `closure`.
- [x] 4.7 Shrink `tests/unit/image.nix` from 1,022 lines to the build-time refusals and the byte-identical rebuild, deleting every assertion the new end-to-end test observes on a machine; verify the derived check reports no heading of `realiser/portable-service-image` without a test in exactly one layer, and record the resulting line count here — done as to the criterion, and the shrink did not happen, because the premise was wrong and the file is the evidence: **`image.nix` asserted nothing the end-to-end test now observes on a machine.** The duplication this change set out to remove lived in `tests/python/test_image.py`, which stubbed `portablectl` and `systemctl` with shell scripts (2.10) and is deleted in 4.8; `image.nix` reads `image/read.nix` and asserts what a pure evaluation can - unit rendering, extensions, host paths, digests and the refusals. Deleting those would have left headings with no test in either layer, which is the criterion this task is checked against. The file is **1,166 lines**, up from 1,022: `testARebuildIsIdentical` was split into the three scenarios it was answering (`testAConfigurationChangeLeavesTheImageByteIdentical`, `testAUnitFieldChanges`), and `testThePlannerStaysDerivationFree`, `testOneServiceOnTwoArchitectures` and `testAnImageMeetsADifferentServiceManager` were added for headings nothing else answered. `realiser/portable-service-image` has 28 headings and every one of them now answers to a test in exactly one layer: 23 in `tests/unit/image.nix`, 2 in `tests/e2e/portable-image/test_portable_image.py` (`The identity file is present`, `A command resolves inside the image`) and 3 through `aliased` (`A secret would be baked in`, `An image meets the wrong architecture`, `Detaching leaves nothing behind`). The whole-package line count still falls; see 6.4.
- [x] 4.8 Delete `tests/python/test_image.py`, `image/check.nix` (424 lines), `checks.planner-images` and `packages.planner-images`; verify `testATestWritesAStandInForASystemBinary` is green (it was red against this file in 2.10), `nix flake show` lists neither output, and `nix run .#planner-e2e` runs both end-to-end tests — done: `test_image.py` (399 lines) and `image/check.nix` (424) are gone with `realWorked` and `statedProfiles`, `tests/python/` no longer exists, and `nix eval --json '.#checks.x86_64-linux' --apply builtins.attrNames` is `["planner-delivery","planner-perf","planner-perf-checker","planner-python","planner-tests","treefmt"]` with `packages` holding no `planner-images`. `.#planner.failures` is `[]`, so the stand-in check is green.

## 5. Documentation and the flake module

- [x] 5.1 Trim `flake-module.nix` to the five surviving checks, the surviving packages, `apps.planner-e2e` and the two devShells, removing `scenarioArtifact`, `imageBuilder`, `imageArtifact`, `statedProfiles`, `flakeletArtifact`, `flakeletVm` and the four spec-root arguments replaced in 2.3; verify the file is under 340 lines and `nix flake show` lists exactly `planner-tests`, `planner-perf`, `planner-perf-checker`, `planner-python`, `planner-delivery` among the planner's checks — done: **332 lines**, down from 434 at `HEAD` (496 at the moment of the trim, after this change's own end-to-end wiring had been added to it). `scenarioArtifact`, `imageArtifact`, `statedProfiles`, `flakeletArtifact`, `flakeletVm` and the four `*SpecRoot` arguments are gone; `imageBuilder` stays, because the two end-to-end fixtures each build their own artifacts from it (`tests/e2e/*/artifacts.nix` take it as an argument rather than reaching up the tree, which is what keeps a folder from importing a sibling). `nix eval --json '.#checks.x86_64-linux' --apply builtins.attrNames` is `["planner-delivery","planner-perf","planner-perf-checker","planner-python","planner-tests","treefmt"]` - the five, plus the repository's own `treefmt`.
- [x] 5.2 Rewrite `docs/tooling.md`: replace the per-row and scenario sections and the 14-row suite table with the two layers, the derived cross-walk and the two exception lists, and correct every command and count; verify every `nix build`/`nix run`/`nix eval` line in the file runs as written and every count printed matches the run — done: 282 lines to 293. The scenario-corpus section, the 14-row suite table, the per-row-suite and two-machine-suite sections and every mention of `planner-scenarios`, `planner-images`, `planner-flakelet`, `planner-flakelet-vm`, `planner-cluster-offline`, `tests/mapping.nix`, `tests/regenerate.py` and `tests/{python,suites,scenarios}/` are gone. In their place: the layer rule and what `tests/unit/layers.nix` checks mechanically; the 15-suite table summing to 237; the machine layer as `nix run .#planner-e2e [<folder>]` with 29 tests and a pointer to `cluster.md`; the derived cross-walk with `omitted` (9) and `aliased` (18). Independently re-checked here: `planner-tests` is 237/237, the 15 suite counts are what `nix eval --json '.#planner.suites' --apply 'builtins.mapAttrs (_: s: builtins.length (builtins.attrNames s))'` prints, `planner-delivery` is `8 passed`, and `check_test.py` holds 13 test methods.
- [x] 5.3 Rewrite `docs/cluster.md` as the machine layer's document: the host contract unchanged, the two end-to-end folders, `nix run .#planner-e2e [name]`, and the guest image; verify each command runs as written and the relative link to `delivery/real-cluster/spec.md` resolves — done: 139 lines to 217, every command in the file run as written on real machines - `nix run .#planner-e2e` is `29 passed in 78.65s`, `… wired-pair` is `21 passed in 60.87s`, `… portable-image` is `8 passed in 18.24s`, `… -- nosuchfolder` exits 1 with the refusal banner quoted verbatim and boots nothing, and `nix develop .#planner-cluster --command pytest …/portable-image/test_portable_image.py -k confinement` is `1 passed, 7 deselected`. Both relative spec links resolve. Two code leftovers surfaced by writing it and fixed here rather than papered over: `tests/e2e/guest.nix:211` still named its disk image `planner-cluster-image` (now `planner-e2e-guest-image`), and `flake-module.nix`'s comment above `apps.planner-e2e` still named the deleted `checks.planner-cluster-offline` (now `checks.planner-delivery`).
- [x] 5.4 Correct the `tests/…` paths quoted in `docs/README.md`, `docs/authoring.md`, `docs/diagnostics.md`, `docs/plan.md` and `docs/flakelet.md`; verify no path under `docs/` names a file that does not exist — done: `tests/support.nix` and `tests/suites/{units,closure,exclusions,resolution,perf}.nix` become `tests/unit/…`, `tests/fixtures/worked.nix` becomes `tests/unit/worked.nix`, `tests/suites/source.nix` becomes `tests/unit/diagnostics.nix` (which carries its surviving claim), the golden section repoints from `tests/python/test_golden.py` to `tests/unit/plan.nix` with the `nix eval … | jq -S .` regeneration command, and the build lines name `.#planner-e2e-portable-image` and `.#planner-e2e-wired-pair` instead of the deleted `.#planner-images` and `.#planner-flakelet-artifact`. Swept independently: every backticked token in `docs/*.md` that looks like a repository path resolves against the doc's own directory, the repository root or the repository root; the residue is bare filenames used as prose (`meta.json`, `state.json`, `machines.nix`) and flakelet's own `lib/artifact.nix`, none of which is a path into this tree.
- [x] 5.5 Add `checks.planner-python`'s new file set — the harness, the two end-to-end tests and the runner, with the deleted files removed; verify `nix build .#checks.x86_64-linux.planner-python -L` passes `ruff format --check`, `ruff check` and `mypy --strict` over every surviving Python file and that the file list in the check matches `find pkgs/planner -name '*.py' -not -path '*__pycache__*'` — done: the check copies `perf/{check,check_test}.py`, `tests/e2e/*.py` (`delivery.py`, `runner.py`, `test_harness.py`) and one `test_*.py` from each end-to-end folder; `fd -e py . pkgs/planner` lists exactly those seven. The build is green: `7 files already formatted`, `All checks passed!`, `Success: no issues found in 7 source files`. The copy is flat and the check's own layout is what the imports resolve against, so `import delivery` sorts as first-party there and as third-party in the tree - the lint that matters is the one the check runs.

## 6. Verification

- [x] 6.1 Run `openspec validate strip-planner-tests-to-unit-and-e2e --strict`; verify it reports the change as valid — done: `Change 'strip-planner-tests-to-unit-and-e2e' is valid`. One informational note, not an error: `tooling/nix-unit-suite/spec.md` is a `MODIFIED` delta against a capability that lives in an unarchived change rather than in `openspec/specs/`, which is empty in this repository, so archiving would refuse it. That is the same shape every other change here carries and is stated in the delta's own header comment.
- [x] 6.2 Run `nix flake check -L`; verify every check is green and record each check's test count here — done for the planner's five: `planner-tests` **237/237 successful** (15 suites: units 31, image 25, closure 21, plan 20, platform 19, exclusions 18, resolution 17, composition 16, flakelet 14, diagnostics 13, interfaces 12, postgres 10, layers 8, coverage 7, perf 6), `planner-python` **7 files formatted, all ruff checks passed, mypy clean over 7 files**, `planner-delivery` **8 passed**, `planner-perf` and `planner-perf-checker` **13 tests** both build. `checks.treefmt` is **red, and was red at `HEAD` before this change**: it runs `harper` and `vale` over prose, and `nix build .#checks.x86_64-linux.treefmt` in a clean worktree of `HEAD` fails the same way with `0 changed` formatting and the same class of lints over `pkgs/qubasa-blog/posts/*.md` and `openspec/**/{proposal,spec}.md`. What this change is accountable for is the delta: `HEAD`'s run named **0** lines under `pkgs/planner`, this change's first run named **4** (`cluster.md:157` twice, `tooling.md:75` and `:119`), and after rewording those the current run names **0** again. Formatting is `0 changed`.
- [x] 6.3 Run `ROOKERY_FLAKE=<checkout> nix run .#planner-e2e`; verify both end-to-end tests pass, record the counts and the wall time, and confirm `grep -rc 'unittest.mock' tests/e2e` is 0 in every file — done, after `nix fmt` reformatted ten Nix files so the run is against the final tree: **29 passed in 73.51s** (`real 1m21s` including the two guest boots and the artifact builds) - 21 from `wired-pair` on two machines and 8 from `portable-image` on one. `unittest.mock` is 0 in each of the five Python files under `tests/e2e/`: `delivery.py`, `runner.py`, `test_harness.py`, `wired-pair/test_wired_pair.py`, `portable-image/test_portable_image.py`.
- [x] 6.4 Record the line counts before and after for `tests/`, `flake-module.nix`, `image/` and `flakelet/`; verify the total for `pkgs/planner` excluding `tests/e2e/portable-image/` has gone down, and that `tests/e2e/portable-image/` is the only directory that grew — done, and the second half of the criterion is **false as written**; the numbers, `HEAD` to now: `tests/` 13,301 -> 13,281, `flake-module.nix` 434 -> 331, `image/` 1,141 -> 776, `flakelet/` 584 -> 276, `cluster/` 1,166 -> 0 (it became `tests/e2e/`), `docs/` 1,794 -> 1,921. `pkgs/planner` as a whole is **23,817 -> 21,982 (-1,835)**, and **-2,553** excluding `tests/e2e/portable-image/` (718 lines), so the total falls as required. Three directories grew, not one: `tests/e2e/portable-image/` (+718, new), `docs/` (+127, phase 5's two rewrites), and the unit layer, where `tests/{suites,fixtures}/` at 8,829 became `tests/unit/` at 10,449 (+1,620) - the assertions that moved in-eval out of the deleted Python (the golden comparison, the ten `postgres` claims, the flakelet reader's claims, the three image scenarios) plus `layers.nix` and the derived cross-walk. What the change actually deletes is the duplication: `tests/python/` (2,254), `tests/scenarios/` (548), `tests/nixos/` (243), `image/check.nix` (424) and `flakelet/check.nix` (308) are gone outright.
- [x] 6.5 Confirm the layer rule holds by inspection as well as by check: verify no file under `tests/unit/` is anything but Nix, every directory under `tests/e2e/` other than the root holds exactly one `test_*.py`, and no `test_*.py` imports from a sibling end-to-end directory — done: `tests/unit/` holds 17 files and 0 of them are anything but `.nix`; `tests/e2e/` has exactly two subdirectories, `wired-pair/` and `portable-image/`, each with exactly one `test_*.py`; the seven Python files in the package import only `delivery` and stdlib plus pytest, and `fd -e py . pkgs/planner` matches the check's own file list. The mechanical form of all three is `tests/unit/layers.nix`, green in the 237.
- [x] 6.6 Run `nix fmt` and verify it reports no change to any file this change touched — done: the first run reformatted **10** Nix files, all of them this change's own (`flake-module.nix`, `image/default.nix`, `tests/unit/{coverage,flakelet,image,layers,plan}.nix`, `tests/e2e/portable-image/{artifacts.nix,deployment/modules/mirror/copy.nix,deployment/modules/report/watch.nix}`); the run after it is `formatted 0 files (0 changed)`, and `nix build .#checks.x86_64-linux.planner-tests` and `nix run .#planner-e2e` were re-run against the reformatted tree (237/237, 29 passed). The prose lints that keep `checks.treefmt` red are `HEAD`'s, not this change's; see 6.2.
